# Auth Free Security Inventory - 2026-05-24

Scope: local repository audit for authentication, bot protection, upload malware scanning, monitoring, and free scanner readiness.

## Evidence Table

| Area | Present? | Evidence | Gap |
| --- | --- | --- | --- |
| Password hashing Argon2id/bcrypt | Partial | Firebase owns primary password auth; password reset calls Firebase from `server/controllers/otpController.js`. OTPs are bcrypt hashed at `server/controllers/otpController.js:38` and `server/controllers/otpController.js:392`. | No repo-visible Argon2id/bcrypt cost for primary passwords because passwords are managed by Firebase. Keep Firebase password policy reviewed; add breach-password checks before reset/signup. |
| MFA / passkeys | Yes | Trusted device schema supports `webauthn` at `server/models/User.js:58`; passkey challenge routes at `server/routes/authRoutes.js:144` and `server/routes/authRoutes.js:149`. | Expand UX to make passkeys the default admin/customer step-up; TOTP is not repo-visible. |
| Rate limiting | Yes | Redis-backed limiter at `server/middleware/distributedRateLimit.js:89`; auth route limiters at `server/routes/authRoutes.js:39`; OTP route limiters at `server/routes/otpRoutes.js:7`. | Add more multi-key account/device/IP-prefix/ASN composition for login pressure beyond selected route-specific keys. |
| Bot challenge | Yes | Turnstile middleware at `server/middleware/turnstileMiddleware.js:27`; OTP routes require Turnstile at `server/routes/otpRoutes.js:40`; PoW challenge code is in `server/controllers/otpController.js:32`. | Keep Turnstile optional/free SaaS; add Anubis or Coraza/NGINX edge PoW if self-hosting. |
| Session rotation | Partial | Redis/browser session storage, revoke, and rotate paths are in `server/services/browserSessionService.js:427`, `server/services/browserSessionService.js:659`, and `server/services/browserSessionService.js:902`. | This is an opaque browser session model, not classic hashed refresh-token-family storage. Add explicit refresh-token reuse detection only if a refresh-token API is introduced. |
| CSRF protection | Yes | Redis-backed one-time CSRF tokens at `server/middleware/csrfMiddleware.js:75`; auth routes validate cookie sessions at `server/routes/authRoutes.js:32` and `server/routes/authRoutes.js:142`. | Keep Redis required for production cookie-auth CSRF. |
| Antivirus scan for uploads | Yes | Common data-URI scan gate at `server/services/uploadSecurityPipeline.js`; review uploads use `server/services/malwareScanService.js` from `server/controllers/uploadController.js`; avatar, listing image, assistant media, visual search, and Gemini remote-media fetch paths now block MIME/magic mismatch, malware, and scan errors before persistence/provider use. | Add quarantine-first async workers if high-volume KYC/support/admin uploads are introduced. |
| Security logs | Yes | Structured auth security event metrics/outbox at `server/services/authSecurityTelemetryService.js:90`; outbox model at `server/models/AuthSecurityEventOutbox.js:3`. | Ship outbox to Loki/OpenSearch/Wazuh in deployed environments. |
| WAF / CRS | Partial | Cloudflare and AWS WAF readiness/docs exist at `docs/cloudflare-security-hardening.md` and `infra/aws/waf-login-security-cloudfront.yml`. | OWASP CRS with ModSecurity/Coraza is not repo-visible. Add self-hosted NGINX + CRS/Coraza config when not relying on Cloudflare/AWS WAF. |
| Dependency scanning | Partial | npm audit gate at `scripts/security-dependency-audit.mjs`; CI security suite at `.github/workflows/ci.yml:356`; added free scanner runner at `scripts/security-free-scanners.mjs`. | Local OSV-Scanner, Trivy, and Semgrep binaries were missing; Docker engine was not running locally. CI can run them through Docker when available. |

## Upload Path Security Audit

| Upload path | Route/controller/service | Accepted type | Size limit | Extension allowlist | MIME allowlist | Magic-byte check | Malware scan gate | Quarantine/pending state | Serving rule | Status | Gap |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Review media uploads | POST `/api/uploads/reviews/sign` + POST `/api/uploads/reviews/upload` (`server/controllers/uploadController.js`) | `data:` image/video base64 | 7MB (`REVIEW_UPLOAD_MAX_BYTES`) | `.jpg`, `.jpeg`, `.png`, `.webp`, `.mp4`, `.webm`, `.mov` by MIME-to-extension storage map | `image/jpeg`, `image/jpg`, `image/png`, `image/webp`, `video/mp4`, `video/webm`, `video/quicktime` | Yes (`detectReviewMediaMime`, `isReviewMediaMimeCompatible`) | Yes (`scanUploadBuffer`) | No pending state; clean-before-write direct storage | `/uploads/reviews/:asset` is served only after upload scan/store succeeds | Protected | None for current direct-storage design |
| Profile avatar data URI | PUT `/api/users/profile` (`server/controllers/userController.js`) | `data:` JPEG/PNG/WebP base64 | 2MB default (`AVATAR_UPLOAD_MAX_BYTES`) | Enforced when filename/extension exists; data URI has no trusted filename | `image/jpeg`, `image/jpg`, `image/png`, `image/webp`; SVG blocked | Yes (`validateImageDataUriUpload`) | Yes (`scanUploadBuffer` through `uploadSecurityPipeline`) | No pending state; clean-before-profile update | Stored avatar value is updated only after clean scan; rejected files are never stored | Protected | Add object storage/quarantine if avatars move out of profile documents |
| Marketplace listing images | POST `/api/listings`, PUT `/api/listings/:id` (`server/controllers/listingController.js`) | `data:` JPEG/PNG/WebP base64; HTTPS or `/uploads` references are URL refs only | 2.5MB default for data URI (`LISTING_IMAGE_MAX_BYTES`) | Enforced when filename/extension exists; generated listing data URIs have no trusted filename | `image/jpeg`, `image/jpg`, `image/png`, `image/webp`; SVG blocked | Yes (`validateImageDataUriUpload`) | Yes (`scanUploadBuffer` through `uploadSecurityPipeline`) | No pending state; clean-before-listing create/update | Listing stores data URI or URL only after clean scan for server-accepted data bytes | Protected seller route | HTTPS URL refs are not fetched or stored by the server in this path |
| Assistant image attachments | POST `/api/ai/chat`, POST `/api/ai/chat/stream` (`server/controllers/aiController.js`) | `data:` JPEG/PNG/WebP base64; URL refs | 8MB default (`ASSISTANT_IMAGE_MAX_BYTES`) | `.jpg`, `.jpeg`, `.png`, `.webp` when filename exists | `image/jpeg`, `image/jpg`, `image/png`, `image/webp`; SVG blocked | Yes (`validateImageDataUriUpload`) | Yes (`scanUploadBuffer` through `uploadSecurityPipeline`) | No storage; in-memory provider input only after clean scan | Not served by app | Protected/private quota path | Direct provider URL refs are not stored; Gemini remote fetch is separately gated below |
| Assistant audio attachments | POST `/api/ai/chat`, POST `/api/ai/chat/stream` (`server/controllers/aiController.js`) | `data:` MP3/WAV/WebM/OGG/M4A/MP4 audio base64; URL refs | 8MB default (`ASSISTANT_AUDIO_MAX_BYTES`) | `.mp3`, `.wav`, `.webm`, `.ogg`, `.m4a`, `.mp4` when filename exists | `audio/mpeg`, `audio/mp3`, `audio/wav`, `audio/x-wav`, `audio/webm`, `audio/ogg`, `audio/mp4`, `audio/m4a` | Yes (audio/container signature checks in `uploadSecurityPipeline`) | Yes (`scanUploadBuffer` through `uploadSecurityPipeline`) | No storage; in-memory provider input only after clean scan | Not served by app | Protected/private quota path | Direct provider URL refs are not stored; Gemini remote fetch is separately gated below |
| Assistant/visual remote media fetch | `fetchRemoteInlineData` in `server/services/ai/geminiGatewayService.js` | Public HTTP(S) image/audio URL fetched server-side | 20MB streaming cap (`MAX_INLINE_MEDIA_BYTES`) | Enforced when URL path includes extension; missing extension allowed if content-type and magic match | Image allowlist or assistant audio allowlist; SVG blocked | Yes (`validateUploadBuffer`) | Yes (`scanUploadBuffer` through `uploadSecurityPipeline`) | No storage; in-memory inline provider payload only after clean scan | Not served by app | Protected by SSRF/DNS/private-IP checks plus scan gate | Other providers may receive URL refs directly without server fetch/storage |
| Visual search image data URI | POST `/api/products/visual-search` (`server/controllers/productController.js`) | `data:` JPEG/PNG/WebP base64; image URL refs | 8MB default (`VISUAL_SEARCH_IMAGE_MAX_BYTES`) | `.jpg`, `.jpeg`, `.png`, `.webp` when filename exists | `image/jpeg`, `image/jpg`, `image/png`, `image/webp`; SVG blocked | Yes (`validateImageDataUriUpload`) | Yes (`scanUploadBuffer` through `uploadSecurityPipeline`) | No storage; in-memory model/search input only after clean scan | Not served by app | Protected route | Image URL refs are not persisted; Gemini remote fetch is gated if provider fetches server-side |
| Support/contact attachments | POST `/api/support`, POST `/api/support/:id/messages` (`server/controllers/supportController.js`) | None; `subject`, `message`, `note`, `mediaMode` text/enums only | Text max 2000 chars | N/A | N/A | N/A | N/A | N/A | N/A | Text-only | Add this same pipeline before introducing support file attachments |
| Admin product uploads | Admin product routes (`server/routes/adminProductRoutes.js`) | URL fields only, no multipart/body file bytes | URL max from validators | N/A | URL schema only | N/A | N/A | N/A | Browser loads external image URL | URL-only | Add this same pipeline before accepting admin file bytes |
| Admin analytics CSV | GET `/api/admin/analytics/export` (`server/controllers/adminAnalyticsController.js`) | Server-generated CSV download | Export size capped by controller | N/A | `text/csv` response | N/A | N/A | N/A | Download generated server-side data only | Export-only, not upload | None |
| Catalog imports / CSV references | POST `/api/admin/catalog/imports`, POST `/api/admin/catalog/onboarding/validate` (`server/controllers/catalogAdminController.js`) | `sourceType`, `sourceRef`, `manifestRef` metadata; no multipart upload | String refs max 500 chars; sample size bounded | Source type enum `json`, `jsonl`, `ndjson`, `csv` | N/A for request body | N/A for request body | N/A for request body | Import job references external/local catalog sources | Imported product image URLs remain URL refs | Reference-based, not body upload | Add scan/quarantine if raw CSV/archive upload endpoint is added |


## Scanner Results

Commands attempted or nearest repo-visible gates:

| Command | Result |
| --- | --- |
| `osv-scanner -r .` | Skipped locally by `npm run security:free-scanners`: `osv-scanner` not installed and Docker engine unavailable. |
| `trivy fs .` | Skipped locally by `npm run security:free-scanners`: `trivy` not installed and Docker engine unavailable. |
| `semgrep --config p/owasp-top-ten .` | Skipped locally by `npm run security:free-scanners`: `semgrep` not installed and Docker engine unavailable. |
| `zap-baseline.py -t $STAGING_URL` | Skipped locally by `npm run security:free-scanners`: `STAGING_URL` is not set; script refuses production/flagged targets by default. |
| `npm run security:deps` | Passed: dependency audit passed for root, app, and server workspaces. |
| `npm run security:cloudflare` | Passed: Cloudflare readiness and Turnstile middleware tests passed. |
| `npm run observability:validate` | Passed: Prometheus alerts, scrape configs, Grafana provisioning, and dashboard are wired. |
| `npm test` | Passed: 29 suites / 317 tests in the server regression tracer. |

## Immediate Follow-Up Backlog

1. Configure ClamAV daemon for staging and set `UPLOAD_MALWARE_SCAN_ENABLED=true`, `CLAMAV_ENABLED=true`, `CLAMAV_HOST`, and `CLAMAV_PORT`.
2. Add `YARA_RULES_PATH` for custom YARA rules after rules are reviewed.
3. Keep support/admin/KYC uploads disabled until they can be added through `uploadSecurityPipeline` or a quarantine-first async worker.
4. Provide a staging URL before running OWASP ZAP baseline.
5. Add OWASP CRS with ModSecurity/Coraza for self-hosted edge deployments, or document the managed WAF equivalence when using Cloudflare/AWS WAF.
