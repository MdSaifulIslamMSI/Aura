# Security/CSP Worktree Recovery Report

Generated after restoring the protected dirty work on branch `codex/security-csp-dirty-work-recovery`.

## Recovery Summary

- Base commit: `03e63fa9d3616eb35634eb7b4ad510827b87e17a`
- Base source: `origin/main`
- Recovery branch: `codex/security-csp-dirty-work-recovery`
- Safety stash applied: `stash@{0}` (`safety-backup-before-main-sync-security-csp-work`)
- Stash status: applied, not popped
- Conflicts: none
- Staged files: none

## Classification

### A. Security/CSP Changes

- `.github/workflows/staging-ops-watch.yml`
  - Adds a live staging OWASP ZAP baseline step and uploads retained DAST artifacts.
- `SECURITY_ARCHITECTURE_REVIEW.md`
  - Adds a dated hardening note so the prior review remains historical rather than misleading.
- `app/config/vercelRoutingContract.mjs`
  - Splits production CSP from local-development CSP.
  - Keeps localhost and `host.docker.internal` out of production `connect-src`.
  - Moves inline style allowance to local-development policy and `style-src-attr`.
- `app/index.html`
  - Updates the static frontend CSP to the hardened production policy.
- `app/src/config/cspPolicy.test.js`
  - Adds assertions for production/local CSP separation and style policy behavior.
- `app/vercel.json`
- `vercel.json`
- `netlify.toml`
  - Align hosted deployment CSP headers with the hardened production policy.
- `app/vite.config.js`
  - Injects the local-development CSP only for Vite dev server mode.
- `infra/aws/docker-compose.ec2.yml`
  - Changes EC2 AI public chat default from opt-out to opt-in.
- `package.json`
  - Enables Windows desktop signing/editing and update signature verification by default.
- `scripts/security-free-scanners.mjs`
  - Makes required ZAP baseline runs fail when no explicit non-production staging target is configured.
- `scripts/security-harness-check.mjs`
  - Adds checks for desktop update signatures, live staging DAST, required ZAP target behavior, and EC2 AI public chat default.
- `server/routes/aiRoutes.js`
  - Marks AI limiters security-critical and disables in-memory fallback in production.
- `server/routes/emailWebhookRoutes.js`
  - Adds a route-specific Resend webhook limiter.
- `server/scripts/audit_production_hardening_contract.js`
  - Updates the production hardening audit to expect signed/verified desktop release defaults.
- `server/tests/aiRateLimitPolicy.test.js`
  - Adds production/non-production AI limiter policy coverage.
- `server/tests/emailWebhookRateLimitPolicy.test.js`
  - Adds production/non-production email webhook limiter policy coverage.
- `server/tests/emailWebhookRoutes.test.js`
  - Keeps webhook route testing isolated from the full app and compatible with the route limiter.
- `docs/security/risk-register.md`
  - Updates R-010 to track retained live staging DAST evidence.
- `docs/staging-operations-upgrades.md`
  - Documents the live staging DAST operation.

### B. Related App Behavior Changes

- `app/src/components/layout/Navbar/index.jsx`
- `app/src/components/layout/Navbar/Navbar.test.jsx`
  - Avoid protected rewards calls until the app session is authenticated.
- `app/src/pages/Home/index.jsx`
  - Suppresses expected optional-auth recommendation failures while preserving fallback behavior.
- `app/src/services/api/recommendationApi.js`
- `app/src/services/api/recommendationApi.test.js`
  - Omits credentials for anonymous optional-auth recommendation calls and preserves credentials for bearer-authenticated calls.

These are related to the security/CSP cleanup because they reduce noisy unauthorized calls and avoid sending cookies on anonymous recommendation requests.

### C. Accidental Localization Changes

None observed in the restored dirty work.

### D. Generated/Cache/Noise Files

- `artifacts/git-safety/*` contains ignored local backup artifacts and must not be committed.
- `app/dist/` may have been regenerated by clean-main build verification and is ignored.

No source files were identified as obvious generated/cache noise.

## Overlap Notes

- `package.json` overlaps by filename with PR #199's localization merge, but the restored hunk is limited to desktop Windows signing/update verification defaults.
- No conflict occurred when applying the stash onto `origin/main`.

## Recommended Commit Plan

If verification passes, stage the coherent security/CSP recovery set in one commit:

`chore(security): preserve pending CSP hardening work`

Recommended staged set:

- All files listed in sections A and B.
- This recovery report may remain uncommitted if the branch should contain only product/security code.
- Do not stage `artifacts/git-safety/*`.

If verification fails, split the branch by failure surface:

1. CSP/deployment header split.
2. AI/webhook rate-limiter policy.
3. Optional-auth frontend request cleanup.
4. Desktop signing/default audit updates.
5. Live staging DAST workflow evidence.
