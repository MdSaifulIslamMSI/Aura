# Auth Unit Test Tier

This folder is reserved for matrix-level unit tests such as password, email, token, role, account-state, and rate-limit policy checks. The executable V1 runs through `server/tests/elasticAuthMatrixArchitecture.test.js` and `server/tests/elasticAuthSecurityProperties.test.js` so it can reuse the existing Jest setup.
