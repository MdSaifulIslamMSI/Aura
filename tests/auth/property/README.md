# Auth Property Test Tier

This folder documents invariant-style auth properties. V1 executes those properties through backend Jest tests using deterministic generated cases, keeping tests provider-free and local-safe.
