# Auth Integration Test Tier

This folder maps the requested integration surfaces: signup/login sync, logout, refresh/session, recovery, email verification, OTP/MFA, and current-user routes. V1 executes the existing backend integration tests through the root `test:auth:*` commands.
