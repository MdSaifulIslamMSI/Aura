# Auth RBAC Test Tier

This folder owns role-route matrix coverage for customer, seller, admin, super admin, support staff, delivery partner, and forbidden route combinations. The generated engine evaluates this matrix through `routeTypes` and `userRoles`.
