apiVersion: v1
kind: Service
metadata:
  name: {{ include "aura-api.fullname" . }}
  labels:
    {{- include "aura-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "aura-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
