apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aura-api.fullname" . }}-config
  labels:
    {{- include "aura-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
