# Railway Dockerfile build context (Root Directory `app`).
# Keep the Caddy build small and deterministic: dependencies and dist/
# are rebuilt inside the image, never shipped as context.
node_modules
dist
coverage
test-results
playwright-report
.run-logs
.env
.env.*
!.env.example
!.env.*.example
*.log
