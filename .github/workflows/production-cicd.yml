name: Manual Production Command Center

on:
  workflow_dispatch:
    inputs:
      confirm_production:
        description: Type PRODUCTION to allow manual production deploy/release/rollback
        required: false
        default: ""
        type: string

      deploy_targets:
        description: "Comma-separated non-overlapping targets: backend, frontend-multihost, gateway"
        required: false
        default: ""
        type: string

      release_targets:
        description: "Comma-separated release targets: desktop, mobile"
        required: false
        default: ""
        type: string

      rollback_targets:
        description: "Comma-separated non-overlapping targets: backend, frontend-multihost, gateway"
        required: false
        default: ""
        type: string

      auto_rollback_on_smoke_failure:
        description: Automatically run rollback hooks if smoke tests fail
        required: false
        default: false
        type: boolean

      desktop_version:
        description: Optional desktop version, for example 1.2.0
        required: false
        default: ""
        type: string

      mobile_version:
        description: Optional mobile version, for example 1.2.0
        required: false
        default: ""
        type: string

      rollback_refs_json:
        description: 'Optional provider-specific refs JSON, for example {"backend":"sha","netlify":"deploy-id","vercel-storefront":"deployment-id","aws-frontend":"snapshot-ref","gateway":"deployment-id"}'
        required: false
        default: "{}"
        type: string

      skip_backend_healthcheck:
        description: Skip backend readiness check after AWS deploy
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: aura-production-mutation
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  NODE_VERSION: 24.x

jobs:
  plan:
    name: Plan production actions
    runs-on: ubuntu-latest
    permissions:
      contents: read

    outputs:
      backend_changed: ${{ steps.plan.outputs.backend_changed }}
      frontend_netlify_changed: ${{ steps.plan.outputs.frontend_netlify_changed }}
      frontend_aws_changed: ${{ steps.plan.outputs.frontend_aws_changed }}
      gateway_changed: ${{ steps.plan.outputs.gateway_changed }}
      desktop_changed: ${{ steps.plan.outputs.desktop_changed }}
      mobile_changed: ${{ steps.plan.outputs.mobile_changed }}
      backend_ready: ${{ steps.plan.outputs.backend_ready }}
      frontend_netlify_ready: ${{ steps.plan.outputs.frontend_netlify_ready }}
      frontend_aws_ready: ${{ steps.plan.outputs.frontend_aws_ready }}
      gateway_ready: ${{ steps.plan.outputs.gateway_ready }}
      backend_missing: ${{ steps.plan.outputs.backend_missing }}
      frontend_netlify_missing: ${{ steps.plan.outputs.frontend_netlify_missing }}
      frontend_aws_missing: ${{ steps.plan.outputs.frontend_aws_missing }}
      gateway_missing: ${{ steps.plan.outputs.gateway_missing }}
      manual_confirmed: ${{ steps.plan.outputs.manual_confirmed }}
      requested_production_action: ${{ steps.plan.outputs.requested_production_action }}
      planned_production_action: ${{ steps.plan.outputs.planned_production_action }}
      requested_deploy_or_release_action: ${{ steps.plan.outputs.requested_deploy_or_release_action }}
      requested_rollback_action: ${{ steps.plan.outputs.requested_rollback_action }}
      should_deploy_backend: ${{ steps.plan.outputs.should_deploy_backend }}
      should_deploy_frontend_netlify: ${{ steps.plan.outputs.should_deploy_frontend_netlify }}
      should_deploy_frontend_aws: ${{ steps.plan.outputs.should_deploy_frontend_aws }}
      should_deploy_gateway: ${{ steps.plan.outputs.should_deploy_gateway }}
      should_release_desktop: ${{ steps.plan.outputs.should_release_desktop }}
      should_release_mobile: ${{ steps.plan.outputs.should_release_mobile }}
      should_rollback_backend: ${{ steps.plan.outputs.should_rollback_backend }}
      should_rollback_frontend_netlify: ${{ steps.plan.outputs.should_rollback_frontend_netlify }}
      should_rollback_frontend_aws: ${{ steps.plan.outputs.should_rollback_frontend_aws }}
      should_rollback_gateway: ${{ steps.plan.outputs.should_rollback_gateway }}
      deploy_backend_requested: ${{ steps.plan.outputs.deploy_backend_requested }}
      deploy_frontend_netlify_requested: ${{ steps.plan.outputs.deploy_frontend_netlify_requested }}
      deploy_frontend_aws_requested: ${{ steps.plan.outputs.deploy_frontend_aws_requested }}
      deploy_gateway_requested: ${{ steps.plan.outputs.deploy_gateway_requested }}
      release_desktop_requested: ${{ steps.plan.outputs.release_desktop_requested }}
      release_mobile_requested: ${{ steps.plan.outputs.release_mobile_requested }}
      should_run_smoke_tests: ${{ steps.plan.outputs.should_run_smoke_tests }}
      signing_enabled: ${{ steps.plan.outputs.signing_enabled }}
      store_publish_enabled: ${{ steps.plan.outputs.store_publish_enabled }}
      rollback_backend_ref: ${{ steps.plan.outputs.rollback_backend_ref }}
      rollback_netlify_ref: ${{ steps.plan.outputs.rollback_netlify_ref }}
      rollback_vercel_storefront_ref: ${{ steps.plan.outputs.rollback_vercel_storefront_ref }}
      rollback_aws_frontend_ref: ${{ steps.plan.outputs.rollback_aws_frontend_ref }}
      rollback_gateway_ref: ${{ steps.plan.outputs.rollback_gateway_ref }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Build production plan
        id: plan
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          REF_NAME: ${{ github.ref_name }}
          BEFORE_SHA: ${{ github.event.before }}
          HEAD_SHA: ${{ github.sha }}
          CONFIRM_PRODUCTION: ${{ github.event.inputs.confirm_production || '' }}
          INPUT_DEPLOY_TARGETS: ${{ github.event.inputs.deploy_targets || '' }}
          INPUT_RELEASE_TARGETS: ${{ github.event.inputs.release_targets || '' }}
          INPUT_ROLLBACK_TARGETS: ${{ github.event.inputs.rollback_targets || '' }}
          INPUT_ROLLBACK_REFS_JSON: ${{ github.event.inputs.rollback_refs_json || '{}' }}
          INPUT_AUTO_ROLLBACK_ON_SMOKE_FAILURE: ${{ github.event.inputs.auto_rollback_on_smoke_failure == 'true' }}
          AWS_DEPLOY_ROLE_ARN: ${{ vars.AWS_DEPLOY_ROLE_ARN || secrets.AWS_DEPLOY_ROLE_ARN || '' }}
          AWS_DEPLOY_BUCKET: ${{ vars.AWS_DEPLOY_BUCKET || '' }}
          AWS_INSTANCE_TAG_VALUE: ${{ vars.AWS_INSTANCE_TAG_VALUE || '' }}
          AWS_PARAMETER_STORE_PATH_PREFIX: ${{ vars.AWS_PARAMETER_STORE_PATH_PREFIX || '' }}
          AURA_BACKEND_ORIGIN: ${{ vars.AURA_BACKEND_ORIGIN || vars.AWS_BACKEND_BASE_URL || '' }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN || '' }}
          NETLIFY_SITE_ID: ${{ vars.NETLIFY_SITE_ID || secrets.NETLIFY_SITE_ID || '' }}
          NETLIFY_PRODUCTION_URL: ${{ vars.NETLIFY_PRODUCTION_URL || '' }}
          AWS_FRONTEND_BUCKET: ${{ vars.AWS_FRONTEND_BUCKET || '' }}
          AWS_FRONTEND_DISTRIBUTION_ID: ${{ vars.AWS_FRONTEND_DISTRIBUTION_ID || '' }}
          AWS_FRONTEND_PUBLIC_URL: ${{ vars.AWS_FRONTEND_PUBLIC_URL || '' }}
          AWS_FRONTEND_DEPLOY_ROLE_ARN: ${{ vars.AWS_FRONTEND_DEPLOY_ROLE_ARN || secrets.AWS_FRONTEND_DEPLOY_ROLE_ARN || '' }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN || '' }}
          VERCEL_ORG_ID: ${{ vars.VERCEL_ORG_ID || secrets.VERCEL_ORG_ID || '' }}
          VERCEL_PROJECT_ID: ${{ vars.VERCEL_PROJECT_ID || secrets.VERCEL_PROJECT_ID || '' }}
          GATEWAY_PRODUCTION_URL: ${{ vars.GATEWAY_PRODUCTION_URL || '' }}
        run: |
          set -euo pipefail

          bool_or() {
            for value in "$@"; do
              if [ "${value}" = "true" ]; then
                echo "true"
                return
              fi
            done
            echo "false"
          }

          emit() {
            echo "$1=$2" >> "$GITHUB_OUTPUT"
          }

          missing_names() {
            local names=()
            local name
            for name in "$@"; do
              if [ -z "${!name:-}" ]; then
                names+=("${name}")
              fi
            done

            if [ "${#names[@]}" -eq 0 ]; then
              printf 'none'
            else
              local IFS=','
              printf '%s' "${names[*]}"
            fi
          }

          ready_from_missing() {
            if [ "$1" = "none" ]; then
              echo "true"
            else
              echo "false"
            fi
          }

          target_selected() {
            local targets="$1"
            shift
            local normalized
            normalized="$(printf '%s' "${targets}" | tr '[:upper:]' '[:lower:]' | tr ';[:space:]' ',,')"

            local IFS=','
            local selected
            local expected
            read -ra selected_targets <<< "${normalized}"
            for selected in "${selected_targets[@]}"; do
              [ -z "${selected}" ] && continue
              for expected in "$@"; do
                if [ "${selected}" = "${expected}" ]; then
                  echo "true"
                  return
                fi
              done
            done

            echo "false"
          }

          validate_target_list() {
            local label="$1"
            local targets="$2"
            shift 2
            local normalized
            normalized="$(printf '%s' "${targets}" | tr '[:upper:]' '[:lower:]' | tr ';[:space:]' ',,')"

            local IFS=','
            local selected
            local expected
            local valid
            read -ra selected_targets <<< "${normalized}"
            for selected in "${selected_targets[@]}"; do
              [ -z "${selected}" ] && continue
              valid="false"
              for expected in "$@"; do
                if [ "${selected}" = "${expected}" ]; then
                  valid="true"
                  break
                fi
              done
              if [ "${valid}" != "true" ]; then
                echo "Unsupported ${label} target '${selected}'." >&2
                return 1
              fi
            done
          }

          rollback_ref_for() {
            ROLLBACK_REF_KEY="$1" ROLLBACK_REFS_JSON="${INPUT_ROLLBACK_REFS_JSON}" node <<'NODE'
          const allowed = new Set(['backend', 'netlify', 'vercel-storefront', 'aws-frontend', 'gateway']);
          let refs;
          try {
            refs = JSON.parse(process.env.ROLLBACK_REFS_JSON || '{}');
          } catch (error) {
            console.error(`rollback_refs_json must be valid JSON: ${error.message}`);
            process.exit(1);
          }
          if (!refs || Array.isArray(refs) || typeof refs !== 'object') {
            console.error('rollback_refs_json must be a JSON object.');
            process.exit(1);
          }
          for (const [key, value] of Object.entries(refs)) {
            if (!allowed.has(key)) {
              console.error(`rollback_refs_json contains unsupported provider '${key}'.`);
              process.exit(1);
            }
            if (typeof value !== 'string' || /[\r\n]/.test(value)) {
              console.error(`rollback_refs_json provider '${key}' must be a single-line string.`);
              process.exit(1);
            }
          }
          process.stdout.write(refs[process.env.ROLLBACK_REF_KEY] || '');
          NODE
          }

          matches_any() {
            local file="$1"
            shift
            local pattern
            for pattern in "$@"; do
              case "${file}" in
                ${pattern}) return 0 ;;
              esac
            done
            return 1
          }

          surface_changed() {
            shift
            local file
            for file in "${changed_files[@]}"; do
              if [ -n "${file}" ] && matches_any "${file}" "$@"; then
                echo "true"
                return
              fi
            done
            echo "false"
          }

          validate_target_list "deploy" "${INPUT_DEPLOY_TARGETS}" backend frontend-multihost storefront gateway
          validate_target_list "release" "${INPUT_RELEASE_TARGETS}" desktop mobile
          validate_target_list "rollback" "${INPUT_ROLLBACK_TARGETS}" backend frontend-multihost storefront gateway

          if [ "${EVENT_NAME}" = "workflow_dispatch" ] && [ "${REF_NAME}" != "main" ] && [ -n "${INPUT_DEPLOY_TARGETS}${INPUT_RELEASE_TARGETS}${INPUT_ROLLBACK_TARGETS}" ]; then
            echo "Production actions must be dispatched from the main branch; received ${REF_NAME}." >&2
            exit 1
          fi

          rollback_backend_ref="$(rollback_ref_for backend)"
          rollback_netlify_ref="$(rollback_ref_for netlify)"
          rollback_vercel_storefront_ref="$(rollback_ref_for vercel-storefront)"
          rollback_aws_frontend_ref="$(rollback_ref_for aws-frontend)"
          rollback_gateway_ref="$(rollback_ref_for gateway)"

          changed_files=()
          if [ "${EVENT_NAME}" = "push" ]; then
            if [ -n "${BEFORE_SHA}" ] && ! printf '%s' "${BEFORE_SHA}" | grep -Eq '^0+$' && git cat-file -e "${BEFORE_SHA}^{commit}" 2>/dev/null; then
              mapfile -t changed_files < <(git diff --name-only "${BEFORE_SHA}" "${HEAD_SHA}")
            else
              mapfile -t changed_files < <(git diff-tree --no-commit-id --name-only -r "${HEAD_SHA}")
            fi
          fi

          backend_changed="$(surface_changed backend \
            'server/*' \
            'infra/aws/*' \
            'scripts/security-*.mjs' \
            'scripts/post-merge-security-smoke.mjs' \
            'scripts/duo-activate.mjs' \
            'scripts/cloudflare-free-security-activate.mjs' \
            'scripts/smoke-origin-protection.mjs' \
            'scripts/smoke-production-login.mjs' \
            'scripts/validate-edge-security-assets.mjs' \
            'scripts/validate-upload-malware-runtime.mjs' \
            'scripts/validate-login-*.mjs' \
            'scripts/run-auth-*.js' \
            'scripts/auth-runner.js' \
            'scripts/classify-auth-change-risk.js' \
            'scripts/count-auth-combinations.js' \
            'tests/auth/*' \
            'docs/*security*.md' \
            'docs/ci-cd.md' \
            'docs/ci-cd-secrets.md' \
            'docs/critical-invariants.md' \
            'docs/emergency-controls.md' \
            'docs/login-staging-production-activation.md' \
            'docs/privileged-access-management.md' \
            'SECURITY_TEST_PLAN.md' \
            'security_best_practices_report.md' \
            '.gitleaks.toml' \
            'security-audit-exceptions.json' \
            'infra/edge/*' \
            'infra/observability/*' \
            'infra/security/*' \
            'docker-compose.split-runtime.yml' \
            '.github/workflows/ci.yml' \
            '.github/workflows/deploy-backend-aws.yml' \
            '.github/workflows/rollback-backend-aws.yml' \
            '.github/workflows/production-cicd.yml')"

          frontend_netlify_changed="$(surface_changed frontend_netlify \
            'app/*' \
            'netlify.toml' \
            '.netlifyignore' \
            'vercel.json' \
            'app/vercel.json' \
            '.vercelignore' \
            'infra/aws/bootstrap-frontend-s3.ps1' \
            'infra/aws/bootstrap-frontend-github-oidc.ps1' \
            'infra/aws/deploy-frontend-s3.ps1' \
            'docs/aws-frontend-deployment.md' \
            'docs/ci-cd-secrets.md' \
            'package.json' \
            'package-lock.json' \
            '.github/workflows/ci.yml' \
            '.github/workflows/deploy-netlify.yml' \
            '.github/workflows/deploy-frontend-aws.yml' \
            '.github/workflows/rollback-netlify.yml' \
            '.github/workflows/rollback-frontend-aws.yml' \
            '.github/workflows/rollback-storefront-vercel.yml' \
            'scripts/rollback-storefront-vercel.sh' \
            '.github/workflows/production-cicd.yml')"

          frontend_aws_changed="$(surface_changed frontend_aws \
            'app/*' \
            'infra/aws/bootstrap-frontend-s3.ps1' \
            'infra/aws/bootstrap-frontend-github-oidc.ps1' \
            'infra/aws/deploy-frontend-s3.ps1' \
            'docs/aws-frontend-deployment.md' \
            'docs/ci-cd-secrets.md' \
            'package.json' \
            'package-lock.json' \
            '.github/workflows/ci.yml' \
            '.github/workflows/deploy-frontend-aws.yml' \
            '.github/workflows/rollback-frontend-aws.yml' \
            '.github/workflows/production-cicd.yml')"

          gateway_changed="$(surface_changed gateway \
            'gateway/*' \
            'gateway/vercel.json' \
            '.vercelignore' \
            'docs/platform-availability.md' \
            'scripts/validate-gateway-release-contract.mjs' \
            'docs/ci-cd-secrets.md' \
            '.github/workflows/ci.yml' \
            '.github/workflows/deploy-gateway-vercel.yml' \
            '.github/workflows/rollback-gateway-vercel.yml' \
            '.github/workflows/production-cicd.yml')"

          desktop_changed="$(surface_changed desktop \
            'app/*' \
            'desktop/*' \
            'docs/ci-cd-secrets.md' \
            'package.json' \
            'package-lock.json' \
            '.github/workflows/ci.yml' \
            '.github/workflows/desktop-release.yml' \
            '.github/workflows/production-cicd.yml')"

          mobile_changed="$(surface_changed mobile \
            'app/capacitor.config.ts' \
            'app/android/*' \
            'app/ios/*' \
            'app/package.json' \
            'app/package-lock.json' \
            'app/src/config/firebase.js' \
            'app/src/context/AuthContext.jsx' \
            'app/src/context/NotificationContext.jsx' \
            'app/src/context/SocketContext.jsx' \
            'app/src/context/VideoCallContext.jsx' \
            'app/src/services/deviceTrustClient.js' \
            'app/src/services/mobileReleaseChannel.js' \
            'app/src/services/nativeAppExperience.js' \
            'app/src/services/nativeSocialAuth.js' \
            'app/src/components/shared/MobileUpdateBanner.jsx' \
            'app/src/utils/nativeRuntime.js' \
            'Gemfile' \
            'Gemfile.lock' \
            'fastlane/*' \
            'docs/mobile-app-delivery.md' \
            'docs/ci-cd-secrets.md' \
            'README.md' \
            '.github/workflows/mobile-release.yml' \
            '.github/workflows/production-cicd.yml')"

          backend_missing="$(missing_names AWS_DEPLOY_ROLE_ARN AWS_DEPLOY_BUCKET AWS_INSTANCE_TAG_VALUE AWS_PARAMETER_STORE_PATH_PREFIX AURA_BACKEND_ORIGIN)"
          frontend_netlify_missing="$(missing_names NETLIFY_AUTH_TOKEN NETLIFY_SITE_ID NETLIFY_PRODUCTION_URL VERCEL_TOKEN VERCEL_ORG_ID VERCEL_PROJECT_ID AWS_FRONTEND_BUCKET AWS_FRONTEND_DISTRIBUTION_ID AWS_FRONTEND_PUBLIC_URL AWS_FRONTEND_DEPLOY_ROLE_ARN AURA_BACKEND_ORIGIN)"
          frontend_aws_missing="$(missing_names AWS_FRONTEND_BUCKET AWS_FRONTEND_DISTRIBUTION_ID AWS_FRONTEND_PUBLIC_URL AWS_FRONTEND_DEPLOY_ROLE_ARN AURA_BACKEND_ORIGIN)"
          gateway_missing="$(missing_names VERCEL_TOKEN VERCEL_ORG_ID VERCEL_PROJECT_ID GATEWAY_PRODUCTION_URL)"

          backend_ready="$(ready_from_missing "${backend_missing}")"
          frontend_netlify_ready="$(ready_from_missing "${frontend_netlify_missing}")"
          frontend_aws_ready="$(ready_from_missing "${frontend_aws_missing}")"
          gateway_ready="$(ready_from_missing "${gateway_missing}")"

          manual_confirmed="false"
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] && [ "${CONFIRM_PRODUCTION}" = "PRODUCTION" ]; then
            manual_confirmed="true"
          fi

          INPUT_DEPLOY_BACKEND="$(target_selected "${INPUT_DEPLOY_TARGETS}" backend)"
          INPUT_DEPLOY_FRONTEND_NETLIFY="$(target_selected "${INPUT_DEPLOY_TARGETS}" frontend-multihost storefront)"
          INPUT_DEPLOY_FRONTEND_AWS="false"
          INPUT_DEPLOY_GATEWAY="$(target_selected "${INPUT_DEPLOY_TARGETS}" gateway)"
          INPUT_RELEASE_DESKTOP="$(target_selected "${INPUT_RELEASE_TARGETS}" desktop)"
          INPUT_RELEASE_MOBILE="$(target_selected "${INPUT_RELEASE_TARGETS}" mobile)"
          INPUT_ROLLBACK_BACKEND="$(target_selected "${INPUT_ROLLBACK_TARGETS}" backend)"
          INPUT_ROLLBACK_FRONTEND_NETLIFY="$(target_selected "${INPUT_ROLLBACK_TARGETS}" frontend-multihost storefront)"
          INPUT_ROLLBACK_FRONTEND_AWS="false"
          INPUT_ROLLBACK_GATEWAY="$(target_selected "${INPUT_ROLLBACK_TARGETS}" gateway)"

          reject_overlapping_lane() {
            local lane="$1"
            local deploy_selected="$2"
            local rollback_selected="$3"
            if [ "${deploy_selected}" = "true" ] && [ "${rollback_selected}" = "true" ]; then
              echo "Production lane '${lane}' cannot be requested for both deploy and rollback in the same run." >&2
              return 1
            fi
          }

          reject_overlapping_lane backend "${INPUT_DEPLOY_BACKEND}" "${INPUT_ROLLBACK_BACKEND}"
          reject_overlapping_lane frontend-multihost "${INPUT_DEPLOY_FRONTEND_NETLIFY}" "${INPUT_ROLLBACK_FRONTEND_NETLIFY}"
          reject_overlapping_lane gateway "${INPUT_DEPLOY_GATEWAY}" "${INPUT_ROLLBACK_GATEWAY}"

          if [ "${INPUT_ROLLBACK_FRONTEND_NETLIFY}" = "true" ]; then
            test -n "${rollback_netlify_ref}" || { echo "frontend-multihost rollback requires rollback_refs_json.netlify." >&2; exit 1; }
            test -n "${rollback_vercel_storefront_ref}" || { echo "frontend-multihost rollback requires rollback_refs_json['vercel-storefront']." >&2; exit 1; }
            test -n "${rollback_aws_frontend_ref}" || { echo "frontend-multihost rollback requires rollback_refs_json['aws-frontend']." >&2; exit 1; }
          fi

          if [ "${INPUT_ROLLBACK_BACKEND}" = "true" ]; then
            [[ "${rollback_backend_ref}" =~ ^[0-9a-f]{40}$ ]] || {
              echo "backend rollback requires rollback_refs_json.backend as a full known-good commit SHA." >&2
              exit 1
            }
          fi

          deploy_backend_requested="false"
          deploy_frontend_netlify_requested="false"
          deploy_frontend_aws_requested="false"
          deploy_gateway_requested="false"
          release_desktop_requested="false"
          release_mobile_requested="false"
          rollback_backend_requested="false"
          rollback_frontend_netlify_requested="false"
          rollback_frontend_aws_requested="false"
          rollback_gateway_requested="false"

          if [ "${EVENT_NAME}" = "push" ]; then
            echo "Main push detected. Running production quality/security gates only; deploys, releases, and rollbacks require workflow_dispatch plus confirm_production=PRODUCTION."
          else
            deploy_backend_requested="${INPUT_DEPLOY_BACKEND}"
            deploy_frontend_netlify_requested="${INPUT_DEPLOY_FRONTEND_NETLIFY}"
            deploy_frontend_aws_requested="${INPUT_DEPLOY_FRONTEND_AWS}"
            deploy_gateway_requested="${INPUT_DEPLOY_GATEWAY}"
            release_desktop_requested="${INPUT_RELEASE_DESKTOP}"
            release_mobile_requested="${INPUT_RELEASE_MOBILE}"
            rollback_backend_requested="${INPUT_ROLLBACK_BACKEND}"
            rollback_frontend_netlify_requested="${INPUT_ROLLBACK_FRONTEND_NETLIFY}"
            rollback_frontend_aws_requested="${INPUT_ROLLBACK_FRONTEND_AWS}"
            rollback_gateway_requested="${INPUT_ROLLBACK_GATEWAY}"
          fi

          should_deploy_backend="false"
          should_deploy_frontend_netlify="false"
          should_deploy_frontend_aws="false"
          should_deploy_gateway="false"
          should_release_desktop="false"
          should_release_mobile="false"
          should_rollback_backend="false"
          should_rollback_frontend_netlify="false"
          should_rollback_frontend_aws="false"
          should_rollback_gateway="false"

          if [ "${EVENT_NAME}" = "push" ]; then
            echo "Skipping automatic production action planning for main push."
          elif [ "${manual_confirmed}" = "true" ]; then
            [ "${INPUT_DEPLOY_BACKEND}" = "true" ] && [ "${backend_ready}" = "true" ] && should_deploy_backend="true"
            [ "${INPUT_DEPLOY_FRONTEND_NETLIFY}" = "true" ] && [ "${frontend_netlify_ready}" = "true" ] && should_deploy_frontend_netlify="true"
            [ "${INPUT_DEPLOY_FRONTEND_AWS}" = "true" ] && [ "${frontend_aws_ready}" = "true" ] && should_deploy_frontend_aws="true"
            [ "${INPUT_DEPLOY_GATEWAY}" = "true" ] && [ "${gateway_ready}" = "true" ] && should_deploy_gateway="true"
            [ "${INPUT_RELEASE_DESKTOP}" = "true" ] && should_release_desktop="true"
            [ "${INPUT_RELEASE_MOBILE}" = "true" ] && should_release_mobile="true"
            [ "${INPUT_ROLLBACK_BACKEND}" = "true" ] && [ "${backend_ready}" = "true" ] && should_rollback_backend="true"
            [ "${INPUT_ROLLBACK_FRONTEND_NETLIFY}" = "true" ] && [ "${frontend_netlify_ready}" = "true" ] && should_rollback_frontend_netlify="true"
            [ "${INPUT_ROLLBACK_FRONTEND_AWS}" = "true" ] && [ "${frontend_aws_ready}" = "true" ] && should_rollback_frontend_aws="true"
            [ "${INPUT_ROLLBACK_GATEWAY}" = "true" ] && [ "${gateway_ready}" = "true" ] && should_rollback_gateway="true"
          fi

          requested_deploy_or_release_action="$(bool_or "${deploy_backend_requested}" "${deploy_frontend_netlify_requested}" "${deploy_frontend_aws_requested}" "${deploy_gateway_requested}" "${release_desktop_requested}" "${release_mobile_requested}")"
          requested_rollback_action="$(bool_or "${rollback_backend_requested}" "${rollback_frontend_netlify_requested}" "${rollback_frontend_aws_requested}" "${rollback_gateway_requested}")"
          requested_production_action="$(bool_or "${requested_deploy_or_release_action}" "${requested_rollback_action}")"
          planned_production_action="$(bool_or "${should_deploy_backend}" "${should_deploy_frontend_netlify}" "${should_deploy_frontend_aws}" "${should_deploy_gateway}" "${should_release_desktop}" "${should_release_mobile}" "${should_rollback_backend}" "${should_rollback_frontend_netlify}" "${should_rollback_frontend_aws}" "${should_rollback_gateway}")"
          should_run_smoke_tests="$(bool_or "${should_deploy_backend}" "${should_deploy_frontend_netlify}" "${should_deploy_frontend_aws}" "${should_deploy_gateway}")"

          emit backend_changed "${backend_changed}"
          emit frontend_netlify_changed "${frontend_netlify_changed}"
          emit frontend_aws_changed "${frontend_aws_changed}"
          emit gateway_changed "${gateway_changed}"
          emit desktop_changed "${desktop_changed}"
          emit mobile_changed "${mobile_changed}"
          emit backend_ready "${backend_ready}"
          emit frontend_netlify_ready "${frontend_netlify_ready}"
          emit frontend_aws_ready "${frontend_aws_ready}"
          emit gateway_ready "${gateway_ready}"
          emit backend_missing "${backend_missing}"
          emit frontend_netlify_missing "${frontend_netlify_missing}"
          emit frontend_aws_missing "${frontend_aws_missing}"
          emit gateway_missing "${gateway_missing}"
          emit manual_confirmed "${manual_confirmed}"
          emit requested_production_action "${requested_production_action}"
          emit planned_production_action "${planned_production_action}"
          emit requested_deploy_or_release_action "${requested_deploy_or_release_action}"
          emit requested_rollback_action "${requested_rollback_action}"
          emit should_deploy_backend "${should_deploy_backend}"
          emit should_deploy_frontend_netlify "${should_deploy_frontend_netlify}"
          emit should_deploy_frontend_aws "${should_deploy_frontend_aws}"
          emit should_deploy_gateway "${should_deploy_gateway}"
          emit should_release_desktop "${should_release_desktop}"
          emit should_release_mobile "${should_release_mobile}"
          emit should_rollback_backend "${should_rollback_backend}"
          emit should_rollback_frontend_netlify "${should_rollback_frontend_netlify}"
          emit should_rollback_frontend_aws "${should_rollback_frontend_aws}"
          emit should_rollback_gateway "${should_rollback_gateway}"
          emit deploy_backend_requested "${deploy_backend_requested}"
          emit deploy_frontend_netlify_requested "${deploy_frontend_netlify_requested}"
          emit deploy_frontend_aws_requested "${deploy_frontend_aws_requested}"
          emit deploy_gateway_requested "${deploy_gateway_requested}"
          emit release_desktop_requested "${release_desktop_requested}"
          emit release_mobile_requested "${release_mobile_requested}"
          emit should_run_smoke_tests "${should_run_smoke_tests}"
          emit signing_enabled "false"
          emit store_publish_enabled "false"
          emit rollback_backend_ref "${rollback_backend_ref}"
          emit rollback_netlify_ref "${rollback_netlify_ref}"
          emit rollback_vercel_storefront_ref "${rollback_vercel_storefront_ref}"
          emit rollback_aws_frontend_ref "${rollback_aws_frontend_ref}"
          emit rollback_gateway_ref "${rollback_gateway_ref}"

          {
            echo "## Production Plan"
            echo
            echo "| Decision | Value |"
            echo "|---|---|"
            echo "| Event | ${EVENT_NAME} |"
            echo "| Changed files | ${#changed_files[@]} |"
            echo "| Manual confirmed | ${manual_confirmed} |"
            echo "| Requested production action | ${requested_production_action} |"
            echo "| Planned production action | ${planned_production_action} |"
            echo "| Signing enabled | false |"
            echo "| Store publishing enabled | false |"
            echo
            echo "| Surface | Changed | Ready | Missing config | Planned action |"
            echo "|---|---:|---:|---|---:|"
            echo "| Backend | ${backend_changed} | ${backend_ready} | ${backend_missing} | ${should_deploy_backend} |"
            echo "| Frontend multihost (Netlify, Vercel, AWS) | ${frontend_netlify_changed} | ${frontend_netlify_ready} | ${frontend_netlify_missing} | ${should_deploy_frontend_netlify} |"
            echo "| Gateway | ${gateway_changed} | ${gateway_ready} | ${gateway_missing} | ${should_deploy_gateway} |"
            echo "| Desktop | ${desktop_changed} | n/a | none | ${should_release_desktop} |"
            echo "| Mobile | ${mobile_changed} | n/a | none | ${should_release_mobile} |"
            echo
            echo "Signing and store publishing are intentionally disabled by this orchestrator. Desktop and mobile release workflows receive explicit false signing/store inputs."
          } >> "$GITHUB_STEP_SUMMARY"

  quality-gates:
    name: Quality gates
    uses: ./.github/workflows/ci.yml
    permissions:
      contents: read
      pull-requests: read

  security-gates:
    name: Security gates and SBOM
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Basic secret pattern scan
        run: |
          set -euo pipefail

          echo "Scanning for dangerous committed secret patterns..."

          if grep -RInE \
            --exclude-dir=.git \
            --exclude-dir=node_modules \
            --exclude-dir=dist \
            --exclude-dir=build \
            --exclude-dir=.next \
            --exclude-dir=.vercel \
            --exclude-dir=.netlify \
            --exclude="*.lock" \
            "(AKIA[0-9A-Z]{16}|-----BEGIN (RSA|OPENSSH|EC|DSA) PRIVATE KEY-----|aws_secret_access_key[[:space:]]*=|AWS_SECRET_ACCESS_KEY[[:space:]]*=|GITHUB_TOKEN[[:space:]]*=[[:space:]]*[A-Za-z0-9_./+-]{20,}|VERCEL_TOKEN[[:space:]]*=[[:space:]]*[A-Za-z0-9_./+-]{20,}|NETLIFY_AUTH_TOKEN[[:space:]]*=[[:space:]]*[A-Za-z0-9_./+-]{20,})" \
            .; then
              echo "::error::Potential committed secret pattern detected. Remove the value and rotate the credential before production deploy."
              exit 1
          fi

          echo "No obvious committed secret patterns found."

      - name: NPM production dependency audit
        run: |
          set -euo pipefail

          audit_npm_dir() {
            local dir="$1"

            if [ -f "${dir}/package-lock.json" ]; then
              echo "Running npm audit in ${dir}"
              (
                cd "${dir}"
                npm audit --audit-level=high --omit=dev
              )
            else
              echo "No package-lock.json in ${dir}; skipping npm audit for this workspace."
            fi
          }

          audit_npm_dir "."
          audit_npm_dir "app"
          audit_npm_dir "gateway"
          audit_npm_dir "server"

      - name: Generate npm SBOMs
        run: |
          set -euo pipefail
          mkdir -p sbom

          sbom_npm_dir() {
            local dir="$1"
            local name="$2"

            if [ -f "${dir}/package-lock.json" ]; then
              echo "Generating SBOM for ${dir}"
              (
                cd "${dir}"
                if ! npm sbom --package-lock-only --legacy-peer-deps --omit=dev --sbom-format cyclonedx > "${GITHUB_WORKSPACE}/sbom/${name}.cdx.json"; then
                  echo "npm sbom failed for ${dir}; generating a package-lock fallback SBOM."
                  SBOM_NAME="${name}" node <<'EOF' > "${GITHUB_WORKSPACE}/sbom/${name}.cdx.json"
          const fs = require('fs');

          const lock = JSON.parse(fs.readFileSync('package-lock.json', 'utf8'));
          const components = Object.entries(lock.packages || {})
            .filter(([packagePath, pkg]) => packagePath && pkg && pkg.dev !== true)
            .map(([packagePath, pkg]) => {
              const fallbackName = packagePath.split('node_modules/').pop() || packagePath;
              return {
                type: 'library',
                name: pkg.name || fallbackName,
                version: pkg.version || '0.0.0',
              };
            });

          process.stdout.write(JSON.stringify({
            bomFormat: 'CycloneDX',
            specVersion: '1.5',
            version: 1,
            metadata: {
              timestamp: new Date().toISOString(),
              component: {
                type: 'application',
                name: lock.name || process.env.SBOM_NAME || 'workspace',
                version: lock.version || '0.0.0',
              },
            },
            components,
          }, null, 2));
          EOF
                fi
              )
            else
              echo "No package-lock.json in ${dir}; skipping SBOM for this workspace."
            fi
          }

          sbom_npm_dir "." "root"
          sbom_npm_dir "app" "app"
          sbom_npm_dir "gateway" "gateway"
          sbom_npm_dir "server" "server"

      - name: Upload SBOMs
        uses: actions/upload-artifact@v7
        with:
          name: aura-production-sbom
          path: sbom/*.json
          if-no-files-found: warn
          retention-days: 90

  release-safety-gates:
    name: Same-SHA staging, cost, observability, and rollback gates
    needs:
      - plan
    if: |
      needs.plan.outputs.planned_production_action == 'true' &&
      needs.plan.outputs.requested_deploy_or_release_action == 'true'
    uses: ./.github/workflows/giant-release-gates.yml
    permissions:
      contents: read
      id-token: write
    secrets:
      STAGING_AWS_DEPLOY_ROLE_ARN: ${{ secrets.STAGING_AWS_DEPLOY_ROLE_ARN }}

  production-approval:
    name: Production approval gate
    runs-on: ubuntu-latest
    needs:
      - plan
      - quality-gates
      - security-gates
      - release-safety-gates
    if: |
      always() &&
      needs.plan.outputs.planned_production_action == 'true' &&
      (
        (
          needs.plan.outputs.requested_deploy_or_release_action != 'true' &&
          needs.plan.outputs.requested_rollback_action == 'true'
        ) ||
        (
          needs.quality-gates.result == 'success' &&
          needs.security-gates.result == 'success' &&
          needs.release-safety-gates.result == 'success'
        )
      )
    environment:
      name: production

    steps:
      - name: Explain approval reason
        env:
          EVENT_NAME: ${{ github.event_name }}
          REQUESTED_DEPLOY_OR_RELEASE: ${{ needs.plan.outputs.requested_deploy_or_release_action }}
          REQUESTED_ROLLBACK: ${{ needs.plan.outputs.requested_rollback_action }}
          QUALITY_GATES_RESULT: ${{ needs.quality-gates.result }}
          SECURITY_GATES_RESULT: ${{ needs.security-gates.result }}
          RELEASE_SAFETY_GATES_RESULT: ${{ needs.release-safety-gates.result }}
          ACTOR_NAME: ${{ github.actor }}
          COMMIT_SHA: ${{ github.sha }}
        run: |
          {
            echo "## Production Approval"
            echo
            echo "Approval passed for a planned production action."
            echo
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Event | ${EVENT_NAME} |"
            echo "| Deploy or release requested | ${REQUESTED_DEPLOY_OR_RELEASE} |"
            echo "| Rollback requested | ${REQUESTED_ROLLBACK} |"
            echo "| Quality gates | ${QUALITY_GATES_RESULT} |"
            echo "| Security gates | ${SECURITY_GATES_RESULT} |"
            echo "| Same-SHA release safety gates | ${RELEASE_SAFETY_GATES_RESULT} |"
            echo "| Actor | ${ACTOR_NAME} |"
            echo "| SHA | ${COMMIT_SHA} |"
          } >> "$GITHUB_STEP_SUMMARY"

  production-approval-noop:
    name: Production approval no-op
    runs-on: ubuntu-latest
    needs:
      - plan
      - quality-gates
      - security-gates
      - release-safety-gates
      - production-approval
    if: always() && needs.production-approval.result != 'success'

    steps:
      - name: Explain why approval did not run
        env:
          PLANNED_PRODUCTION_ACTION: ${{ needs.plan.outputs.planned_production_action }}
          REQUESTED_PRODUCTION_ACTION: ${{ needs.plan.outputs.requested_production_action }}
          REQUESTED_DEPLOY_OR_RELEASE: ${{ needs.plan.outputs.requested_deploy_or_release_action }}
          MANUAL_CONFIRMED: ${{ needs.plan.outputs.manual_confirmed }}
          QUALITY_GATES_RESULT: ${{ needs.quality-gates.result }}
          SECURITY_GATES_RESULT: ${{ needs.security-gates.result }}
          RELEASE_SAFETY_GATES_RESULT: ${{ needs.release-safety-gates.result }}
          APPROVAL_RESULT: ${{ needs.production-approval.result }}
        run: |
          {
            echo "## Production Approval No-Op"
            echo
            echo "| Reason | Value |"
            echo "|---|---|"
            echo "| Planned production action | ${PLANNED_PRODUCTION_ACTION} |"
            echo "| Requested production action | ${REQUESTED_PRODUCTION_ACTION} |"
            echo "| Manual confirmed | ${MANUAL_CONFIRMED} |"
            echo "| Quality gates | ${QUALITY_GATES_RESULT} |"
            echo "| Security gates | ${SECURITY_GATES_RESULT} |"
            echo "| Same-SHA release safety gates | ${RELEASE_SAFETY_GATES_RESULT} |"
            echo "| Approval result | ${APPROVAL_RESULT} |"
            echo
            if [ "${PLANNED_PRODUCTION_ACTION}" != "true" ]; then
              echo "No real production action was planned, so the production environment approval was intentionally not requested."
            elif [ "${REQUESTED_DEPLOY_OR_RELEASE}" = "true" ] && { [ "${QUALITY_GATES_RESULT}" != "success" ] || [ "${SECURITY_GATES_RESULT}" != "success" ] || [ "${RELEASE_SAFETY_GATES_RESULT}" != "success" ]; }; then
              echo "A deploy or release was requested, but production approval was blocked because CI, security, or same-SHA release safety gates did not pass."
            else
              echo "Production approval did not complete successfully."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  release-manifest:
    name: Create release manifest
    runs-on: ubuntu-latest
    needs:
      - plan
      - production-approval
    if: always() && needs.production-approval.result == 'success'
    permissions:
      contents: read

    steps:
      - name: Create manifest
        env:
          EVENT_NAME: ${{ github.event_name }}
          ACTOR: ${{ github.actor }}
          REF_NAME: ${{ github.ref }}
          SHA: ${{ github.sha }}
          RUN_ID: ${{ github.run_id }}
          BACKEND_CHANGED: ${{ needs.plan.outputs.backend_changed }}
          FRONTEND_NETLIFY_CHANGED: ${{ needs.plan.outputs.frontend_netlify_changed }}
          FRONTEND_AWS_CHANGED: ${{ needs.plan.outputs.frontend_aws_changed }}
          GATEWAY_CHANGED: ${{ needs.plan.outputs.gateway_changed }}
          DESKTOP_CHANGED: ${{ needs.plan.outputs.desktop_changed }}
          MOBILE_CHANGED: ${{ needs.plan.outputs.mobile_changed }}
          BACKEND_READY: ${{ needs.plan.outputs.backend_ready }}
          FRONTEND_NETLIFY_READY: ${{ needs.plan.outputs.frontend_netlify_ready }}
          FRONTEND_AWS_READY: ${{ needs.plan.outputs.frontend_aws_ready }}
          GATEWAY_READY: ${{ needs.plan.outputs.gateway_ready }}
          SHOULD_DEPLOY_BACKEND: ${{ needs.plan.outputs.should_deploy_backend }}
          SHOULD_DEPLOY_FRONTEND_NETLIFY: ${{ needs.plan.outputs.should_deploy_frontend_netlify }}
          SHOULD_DEPLOY_FRONTEND_AWS: ${{ needs.plan.outputs.should_deploy_frontend_aws }}
          SHOULD_DEPLOY_GATEWAY: ${{ needs.plan.outputs.should_deploy_gateway }}
          SHOULD_RELEASE_DESKTOP: ${{ needs.plan.outputs.should_release_desktop }}
          SHOULD_RELEASE_MOBILE: ${{ needs.plan.outputs.should_release_mobile }}
          SHOULD_ROLLBACK_BACKEND: ${{ needs.plan.outputs.should_rollback_backend }}
          SHOULD_ROLLBACK_FRONTEND_NETLIFY: ${{ needs.plan.outputs.should_rollback_frontend_netlify }}
          SHOULD_ROLLBACK_FRONTEND_AWS: ${{ needs.plan.outputs.should_rollback_frontend_aws }}
          SHOULD_ROLLBACK_GATEWAY: ${{ needs.plan.outputs.should_rollback_gateway }}
          SIGNING_ENABLED: ${{ needs.plan.outputs.signing_enabled }}
          STORE_PUBLISH_ENABLED: ${{ needs.plan.outputs.store_publish_enabled }}
        run: |
          node <<'EOF'
          const fs = require('fs');

          const bool = (name) => process.env[name] === 'true';
          const manifest = {
            project: 'Aura',
            workflow: 'Aura Production Release Orchestrator',
            event: process.env.EVENT_NAME,
            actor: process.env.ACTOR,
            ref: process.env.REF_NAME,
            sha: process.env.SHA,
            runId: process.env.RUN_ID,
            changed: {
              backend: bool('BACKEND_CHANGED'),
              frontendMultihost: bool('FRONTEND_NETLIFY_CHANGED'),
              gateway: bool('GATEWAY_CHANGED'),
              desktop: bool('DESKTOP_CHANGED'),
              mobile: bool('MOBILE_CHANGED'),
            },
            providerReadiness: {
              backend: bool('BACKEND_READY'),
              frontendMultihost: bool('FRONTEND_NETLIFY_READY'),
              gateway: bool('GATEWAY_READY'),
            },
            plannedActions: {
              deployBackend: bool('SHOULD_DEPLOY_BACKEND'),
              deployFrontendMultihost: bool('SHOULD_DEPLOY_FRONTEND_NETLIFY'),
              deployGateway: bool('SHOULD_DEPLOY_GATEWAY'),
              releaseDesktop: bool('SHOULD_RELEASE_DESKTOP'),
              releaseMobile: bool('SHOULD_RELEASE_MOBILE'),
              rollbackBackend: bool('SHOULD_ROLLBACK_BACKEND'),
              rollbackFrontendMultihost: bool('SHOULD_ROLLBACK_FRONTEND_NETLIFY'),
              rollbackGateway: bool('SHOULD_ROLLBACK_GATEWAY'),
            },
            signing: {
              enabled: bool('SIGNING_ENABLED'),
              requireWindowsSigning: false,
              requireMacosSigning: false,
              requireAndroidSigning: false,
              requireIosSigning: false,
            },
            storePublishing: {
              enabled: bool('STORE_PUBLISH_ENABLED'),
              publishStoreRelease: false,
            },
          };

          fs.writeFileSync('release-manifest.json', `${JSON.stringify(manifest, null, 2)}\n`);
          console.log(JSON.stringify(manifest, null, 2));
          EOF

      - name: Upload release manifest
        uses: actions/upload-artifact@v7
        with:
          name: aura-production-release-manifest
          path: release-manifest.json
          retention-days: 90

  deploy-backend:
    name: Deploy backend to AWS
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - release-manifest
    if: |
      always() &&
      needs.plan.outputs.should_deploy_backend == 'true' &&
      needs.quality-gates.result == 'success' &&
      needs.security-gates.result == 'success' &&
      needs.production-approval.result == 'success' &&
      needs.release-manifest.result == 'success'
    uses: ./.github/workflows/deploy-backend-aws.yml
    permissions:
      contents: read
      id-token: write
    with:
      git_ref: ${{ github.sha }}
      skip_healthcheck: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.skip_backend_healthcheck == 'true' || false }}
    secrets:
      AWS_DEPLOY_ROLE_ARN: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}

  deploy-backend-noop:
    name: Deploy backend no-op
    runs-on: ubuntu-latest
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - deploy-backend
    if: always() && needs.deploy-backend.result == 'skipped'

    steps:
      - name: Explain backend deploy decision
        env:
          EVENT_NAME: ${{ github.event_name }}
          CHANGED: ${{ needs.plan.outputs.backend_changed }}
          READY: ${{ needs.plan.outputs.backend_ready }}
          MISSING: ${{ needs.plan.outputs.backend_missing }}
          SHOULD: ${{ needs.plan.outputs.should_deploy_backend }}
          MANUAL_CONFIRMED: ${{ needs.plan.outputs.manual_confirmed }}
          MANUAL_REQUESTED: ${{ needs.plan.outputs.deploy_backend_requested }}
          QUALITY_GATES_RESULT: ${{ needs.quality-gates.result }}
          SECURITY_GATES_RESULT: ${{ needs.security-gates.result }}
          APPROVAL_RESULT: ${{ needs.production-approval.result }}
        run: |
          {
            echo "## Backend Deploy No-Op"
            echo
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Event | ${EVENT_NAME} |"
            echo "| Changed | ${CHANGED} |"
            echo "| Manual requested | ${MANUAL_REQUESTED} |"
            echo "| Manual confirmed | ${MANUAL_CONFIRMED} |"
            echo "| Ready | ${READY} |"
            echo "| Missing config | ${MISSING} |"
            echo "| Planned deploy | ${SHOULD} |"
            echo "| Quality gates | ${QUALITY_GATES_RESULT} |"
            echo "| Security gates | ${SECURITY_GATES_RESULT} |"
            echo "| Approval | ${APPROVAL_RESULT} |"
            echo
            if [ "${READY}" != "true" ] && { [ "${CHANGED}" = "true" ] || [ "${MANUAL_REQUESTED}" = "true" ]; }; then
              echo "Backend deploy was requested but no-opped because required configuration is missing: ${MISSING}."
            elif [ "${EVENT_NAME}" = "workflow_dispatch" ] && [ "${MANUAL_REQUESTED}" = "true" ] && [ "${MANUAL_CONFIRMED}" != "true" ]; then
              echo "Backend deploy was requested but no-opped because confirm_production was not PRODUCTION."
            elif [ "${QUALITY_GATES_RESULT}" != "success" ] || [ "${SECURITY_GATES_RESULT}" != "success" ]; then
              echo "Backend deploy was blocked because CI or security gates did not pass."
            elif [ "${SHOULD}" != "true" ]; then
              echo "Backend deploy was not requested for this run."
            else
              echo "Backend deploy did not run because production approval or release manifest did not complete."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  deploy-frontend-netlify:
    name: Deploy storefront to Netlify, Vercel, and AWS
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - release-manifest
    if: |
      always() &&
      needs.plan.outputs.should_deploy_frontend_netlify == 'true' &&
      needs.quality-gates.result == 'success' &&
      needs.security-gates.result == 'success' &&
      needs.production-approval.result == 'success' &&
      needs.release-manifest.result == 'success'
    uses: ./.github/workflows/deploy-netlify.yml
    permissions:
      contents: read
      id-token: write
      issues: write
      pull-requests: write
    with:
      target: production
      auto_rollback_on_failure: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.auto_rollback_on_smoke_failure == 'true' || false }}
      parent_holds_production_lock: true
    secrets:
      AWS_FRONTEND_DEPLOY_ROLE_ARN: ${{ secrets.AWS_FRONTEND_DEPLOY_ROLE_ARN }}
      NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
      VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
      VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
      VITE_FIREBASE_CONFIG: ${{ secrets.VITE_FIREBASE_CONFIG }}
      VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}
      VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
      VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
      VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
      VITE_FIREBASE_WEB_CONFIG: ${{ secrets.VITE_FIREBASE_WEB_CONFIG }}

  deploy-frontend-netlify-noop:
    name: Deploy Netlify frontend no-op
    runs-on: ubuntu-latest
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - deploy-frontend-netlify
    if: always() && needs.deploy-frontend-netlify.result == 'skipped'

    steps:
      - name: Explain Netlify frontend deploy decision
        env:
          EVENT_NAME: ${{ github.event_name }}
          CHANGED: ${{ needs.plan.outputs.frontend_netlify_changed }}
          READY: ${{ needs.plan.outputs.frontend_netlify_ready }}
          MISSING: ${{ needs.plan.outputs.frontend_netlify_missing }}
          SHOULD: ${{ needs.plan.outputs.should_deploy_frontend_netlify }}
          MANUAL_CONFIRMED: ${{ needs.plan.outputs.manual_confirmed }}
          MANUAL_REQUESTED: ${{ needs.plan.outputs.deploy_frontend_netlify_requested }}
          QUALITY_GATES_RESULT: ${{ needs.quality-gates.result }}
          SECURITY_GATES_RESULT: ${{ needs.security-gates.result }}
          APPROVAL_RESULT: ${{ needs.production-approval.result }}
        run: |
          {
            echo "## Netlify Frontend Deploy No-Op"
            echo
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Event | ${EVENT_NAME} |"
            echo "| Changed | ${CHANGED} |"
            echo "| Manual requested | ${MANUAL_REQUESTED} |"
            echo "| Manual confirmed | ${MANUAL_CONFIRMED} |"
            echo "| Ready | ${READY} |"
            echo "| Missing config | ${MISSING} |"
            echo "| Planned deploy | ${SHOULD} |"
            echo "| Quality gates | ${QUALITY_GATES_RESULT} |"
            echo "| Security gates | ${SECURITY_GATES_RESULT} |"
            echo "| Approval | ${APPROVAL_RESULT} |"
            echo
            if [ "${READY}" != "true" ] && { [ "${CHANGED}" = "true" ] || [ "${MANUAL_REQUESTED}" = "true" ]; }; then
              echo "Netlify frontend deploy was requested but no-opped because required configuration is missing: ${MISSING}."
            elif [ "${EVENT_NAME}" = "workflow_dispatch" ] && [ "${MANUAL_REQUESTED}" = "true" ] && [ "${MANUAL_CONFIRMED}" != "true" ]; then
              echo "Netlify frontend deploy was requested but no-opped because confirm_production was not PRODUCTION."
            elif [ "${QUALITY_GATES_RESULT}" != "success" ] || [ "${SECURITY_GATES_RESULT}" != "success" ]; then
              echo "Netlify frontend deploy was blocked because CI or security gates did not pass."
            elif [ "${SHOULD}" != "true" ]; then
              echo "Netlify frontend deploy was not requested for this run."
            else
              echo "Netlify frontend deploy did not run because production approval or release manifest did not complete."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  deploy-frontend-aws:
    name: Deploy storefront to AWS S3 / CloudFront
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - release-manifest
    if: |
      always() &&
      needs.plan.outputs.should_deploy_frontend_aws == 'true' &&
      needs.quality-gates.result == 'success' &&
      needs.security-gates.result == 'success' &&
      needs.production-approval.result == 'success' &&
      needs.release-manifest.result == 'success'
    uses: ./.github/workflows/deploy-frontend-aws.yml
    permissions:
      contents: read
      id-token: write
    with:
      deploy: true
      parent_holds_production_lock: true
    secrets:
      AWS_FRONTEND_DEPLOY_ROLE_ARN: ${{ secrets.AWS_FRONTEND_DEPLOY_ROLE_ARN }}
      VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
      VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
      VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
      VITE_FIREBASE_CONFIG: ${{ secrets.VITE_FIREBASE_CONFIG }}
      VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}
      VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
      VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
      VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
      VITE_FIREBASE_WEB_CONFIG: ${{ secrets.VITE_FIREBASE_WEB_CONFIG }}

  deploy-frontend-aws-noop:
    name: Deploy AWS frontend no-op
    runs-on: ubuntu-latest
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - deploy-frontend-aws
    if: always() && needs.deploy-frontend-aws.result == 'skipped'

    steps:
      - name: Explain AWS frontend deploy decision
        env:
          EVENT_NAME: ${{ github.event_name }}
          CHANGED: ${{ needs.plan.outputs.frontend_aws_changed }}
          READY: ${{ needs.plan.outputs.frontend_aws_ready }}
          MISSING: ${{ needs.plan.outputs.frontend_aws_missing }}
          SHOULD: ${{ needs.plan.outputs.should_deploy_frontend_aws }}
          MANUAL_CONFIRMED: ${{ needs.plan.outputs.manual_confirmed }}
          MANUAL_REQUESTED: ${{ needs.plan.outputs.deploy_frontend_aws_requested }}
          QUALITY_GATES_RESULT: ${{ needs.quality-gates.result }}
          SECURITY_GATES_RESULT: ${{ needs.security-gates.result }}
          APPROVAL_RESULT: ${{ needs.production-approval.result }}
        run: |
          {
            echo "## AWS Frontend Deploy No-Op"
            echo
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Event | ${EVENT_NAME} |"
            echo "| Changed | ${CHANGED} |"
            echo "| Manual requested | ${MANUAL_REQUESTED} |"
            echo "| Manual confirmed | ${MANUAL_CONFIRMED} |"
            echo "| Ready | ${READY} |"
            echo "| Missing config | ${MISSING} |"
            echo "| Planned deploy | ${SHOULD} |"
            echo "| Quality gates | ${QUALITY_GATES_RESULT} |"
            echo "| Security gates | ${SECURITY_GATES_RESULT} |"
            echo "| Approval | ${APPROVAL_RESULT} |"
            echo
            if [ "${READY}" != "true" ] && { [ "${CHANGED}" = "true" ] || [ "${MANUAL_REQUESTED}" = "true" ]; }; then
              echo "AWS frontend deploy was requested but no-opped because required configuration is missing: ${MISSING}."
            elif [ "${EVENT_NAME}" = "workflow_dispatch" ] && [ "${MANUAL_REQUESTED}" = "true" ] && [ "${MANUAL_CONFIRMED}" != "true" ]; then
              echo "AWS frontend deploy was requested but no-opped because confirm_production was not PRODUCTION."
            elif [ "${QUALITY_GATES_RESULT}" != "success" ] || [ "${SECURITY_GATES_RESULT}" != "success" ]; then
              echo "AWS frontend deploy was blocked because CI or security gates did not pass."
            elif [ "${SHOULD}" != "true" ]; then
              echo "AWS frontend deploy was not requested for this run."
            else
              echo "AWS frontend deploy did not run because production approval or release manifest did not complete."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  deploy-gateway:
    name: Deploy gateway to Vercel
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - release-manifest
    if: |
      always() &&
      needs.plan.outputs.should_deploy_gateway == 'true' &&
      needs.quality-gates.result == 'success' &&
      needs.security-gates.result == 'success' &&
      needs.production-approval.result == 'success' &&
      needs.release-manifest.result == 'success'
    uses: ./.github/workflows/deploy-gateway-vercel.yml
    permissions:
      contents: read
      issues: write
      pull-requests: write
    with:
      target: production
      parent_holds_production_lock: true
    secrets:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

  deploy-gateway-noop:
    name: Deploy gateway no-op
    runs-on: ubuntu-latest
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - deploy-gateway
    if: always() && needs.deploy-gateway.result == 'skipped'

    steps:
      - name: Explain gateway deploy decision
        env:
          EVENT_NAME: ${{ github.event_name }}
          CHANGED: ${{ needs.plan.outputs.gateway_changed }}
          READY: ${{ needs.plan.outputs.gateway_ready }}
          MISSING: ${{ needs.plan.outputs.gateway_missing }}
          SHOULD: ${{ needs.plan.outputs.should_deploy_gateway }}
          MANUAL_CONFIRMED: ${{ needs.plan.outputs.manual_confirmed }}
          MANUAL_REQUESTED: ${{ needs.plan.outputs.deploy_gateway_requested }}
          QUALITY_GATES_RESULT: ${{ needs.quality-gates.result }}
          SECURITY_GATES_RESULT: ${{ needs.security-gates.result }}
          APPROVAL_RESULT: ${{ needs.production-approval.result }}
        run: |
          {
            echo "## Gateway Deploy No-Op"
            echo
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Event | ${EVENT_NAME} |"
            echo "| Changed | ${CHANGED} |"
            echo "| Manual requested | ${MANUAL_REQUESTED} |"
            echo "| Manual confirmed | ${MANUAL_CONFIRMED} |"
            echo "| Ready | ${READY} |"
            echo "| Missing config | ${MISSING} |"
            echo "| Planned deploy | ${SHOULD} |"
            echo "| Quality gates | ${QUALITY_GATES_RESULT} |"
            echo "| Security gates | ${SECURITY_GATES_RESULT} |"
            echo "| Approval | ${APPROVAL_RESULT} |"
            echo
            if [ "${READY}" != "true" ] && { [ "${CHANGED}" = "true" ] || [ "${MANUAL_REQUESTED}" = "true" ]; }; then
              echo "Gateway deploy was requested but no-opped because required configuration is missing: ${MISSING}."
            elif [ "${EVENT_NAME}" = "workflow_dispatch" ] && [ "${MANUAL_REQUESTED}" = "true" ] && [ "${MANUAL_CONFIRMED}" != "true" ]; then
              echo "Gateway deploy was requested but no-opped because confirm_production was not PRODUCTION."
            elif [ "${QUALITY_GATES_RESULT}" != "success" ] || [ "${SECURITY_GATES_RESULT}" != "success" ]; then
              echo "Gateway deploy was blocked because CI or security gates did not pass."
            elif [ "${SHOULD}" != "true" ]; then
              echo "Gateway deploy was not requested for this run."
            else
              echo "Gateway deploy did not run because production approval or release manifest did not complete."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  smoke-tests:
    name: Production smoke tests
    runs-on: ubuntu-latest
    needs:
      - deploy-backend
      - deploy-frontend-netlify
      - deploy-frontend-aws
      - deploy-gateway
    if: |
      always() &&
      (
        needs.deploy-backend.result == 'success' ||
        needs.deploy-frontend-netlify.result == 'success' ||
        needs.deploy-frontend-aws.result == 'success' ||
        needs.deploy-gateway.result == 'success'
      )

    steps:
      - uses: actions/checkout@v6

      - name: Use Node.js 24.x
        uses: actions/setup-node@v7
        with:
          node-version: 24.x

      - name: Preflight production smoke target
        env:
          SMOKE_TARGET_ENV: production
          ALLOW_PRODUCTION_SMOKE: "true"
          SMOKE_BASE_URL: ${{ vars.AURA_BACKEND_ORIGIN || vars.AWS_BACKEND_BASE_URL || vars.AWS_FRONTEND_PUBLIC_URL || vars.NETLIFY_PRODUCTION_URL || vars.GATEWAY_PRODUCTION_URL || '' }}
          PROD_BASE_URL: ${{ vars.AWS_FRONTEND_PUBLIC_URL || vars.NETLIFY_PRODUCTION_URL || vars.GATEWAY_PRODUCTION_URL || '' }}
          PROD_API_BASE_URL: ${{ vars.AURA_BACKEND_ORIGIN || vars.AWS_BACKEND_BASE_URL || '' }}
          PROD_SSM_PREFIX: /aura/prod
        run: node scripts/smoke-preflight.mjs

      - name: Run production smoke checks
        env:
          BACKEND_DEPLOY_RESULT: ${{ needs.deploy-backend.result }}
          FRONTEND_NETLIFY_DEPLOY_RESULT: ${{ needs.deploy-frontend-netlify.result }}
          GATEWAY_DEPLOY_RESULT: ${{ needs.deploy-gateway.result }}
          AURA_BACKEND_ORIGIN: ${{ vars.AURA_BACKEND_ORIGIN || vars.AWS_BACKEND_BASE_URL || '' }}
          BACKEND_HEALTH_PATH: ${{ vars.BACKEND_HEALTH_PATH || '/health' }}
          NETLIFY_PRODUCTION_URL: ${{ needs.deploy-frontend-netlify.outputs.netlify_production_url || vars.NETLIFY_PRODUCTION_URL || '' }}
          VERCEL_STOREFRONT_PRODUCTION_URL: ${{ needs.deploy-frontend-netlify.outputs.vercel_storefront_production_url || vars.VERCEL_PRODUCTION_URL || '' }}
          AWS_FRONTEND_PUBLIC_URL: ${{ needs.deploy-frontend-netlify.outputs.aws_frontend_production_url || vars.AWS_FRONTEND_PUBLIC_URL || '' }}
          GATEWAY_PRODUCTION_URL: ${{ vars.GATEWAY_PRODUCTION_URL || '' }}
          GATEWAY_HEALTH_PATH: ${{ vars.GATEWAY_HEALTH_PATH || '/' }}
        run: |
          set -euo pipefail

          smoke_if_deployed() {
            local name="$1"
            local deploy_result="$2"
            local url="$3"

            if [ "${deploy_result}" != "success" ]; then
              echo "Skipping ${name}; related deploy result was ${deploy_result}."
              return 0
            fi

            if [ -z "${url}" ]; then
              echo "${name} was deployed but its production URL is empty." >&2
              return 1
            fi

            echo "Checking ${name}: ${url}"
            curl --fail --show-error --silent --location --max-time 30 "${url}" > /dev/null
            echo "${name} is healthy."
          }

          smoke_if_deployed "backend" "${BACKEND_DEPLOY_RESULT}" "${AURA_BACKEND_ORIGIN}${BACKEND_HEALTH_PATH}"
          smoke_if_deployed "netlify storefront" "${FRONTEND_NETLIFY_DEPLOY_RESULT}" "${NETLIFY_PRODUCTION_URL}"
          smoke_if_deployed "vercel storefront" "${FRONTEND_NETLIFY_DEPLOY_RESULT}" "${VERCEL_STOREFRONT_PRODUCTION_URL}"
          smoke_if_deployed "aws storefront" "${FRONTEND_NETLIFY_DEPLOY_RESULT}" "${AWS_FRONTEND_PUBLIC_URL}"
          smoke_if_deployed "gateway" "${GATEWAY_DEPLOY_RESULT}" "${GATEWAY_PRODUCTION_URL}${GATEWAY_HEALTH_PATH}"

  smoke-tests-noop:
    name: Smoke tests no-op
    runs-on: ubuntu-latest
    needs:
      - deploy-backend
      - deploy-frontend-netlify
      - deploy-frontend-aws
      - deploy-gateway
      - smoke-tests
    if: always() && needs.smoke-tests.result == 'skipped'

    steps:
      - name: Explain smoke decision
        env:
          BACKEND_RESULT: ${{ needs.deploy-backend.result }}
          FRONTEND_NETLIFY_RESULT: ${{ needs.deploy-frontend-netlify.result }}
          FRONTEND_AWS_RESULT: ${{ needs.deploy-frontend-aws.result }}
          GATEWAY_RESULT: ${{ needs.deploy-gateway.result }}
        run: |
          {
            echo "## Smoke Tests No-Op"
            echo
            echo "No smoke tests ran because no deployment job completed successfully."
            echo
            echo "| Deploy job | Result |"
            echo "|---|---|"
            echo "| Backend | ${BACKEND_RESULT} |"
            echo "| Frontend Netlify | ${FRONTEND_NETLIFY_RESULT} |"
            echo "| Frontend AWS | ${FRONTEND_AWS_RESULT} |"
            echo "| Gateway | ${GATEWAY_RESULT} |"
          } >> "$GITHUB_STEP_SUMMARY"

  rollback-backend:
    name: Roll back backend
    needs:
      - plan
      - production-approval
      - deploy-backend
      - smoke-tests
    if: |
      always() &&
      needs.production-approval.result == 'success' &&
      (
        needs.plan.outputs.should_rollback_backend == 'true' ||
        (
          github.event_name == 'workflow_dispatch' &&
          github.event.inputs.auto_rollback_on_smoke_failure == 'true' &&
          needs.deploy-backend.result == 'success' &&
          needs.smoke-tests.result == 'failure'
        )
      )
    uses: ./.github/workflows/rollback-backend-aws.yml
    permissions:
      contents: read
      id-token: write
    with:
      rollback_ref: ${{ needs.plan.outputs.rollback_backend_ref != '' && needs.plan.outputs.rollback_backend_ref || needs.deploy-backend.outputs.backend_rollback_ref }}
      execute_rollback: true
    secrets:
      AWS_DEPLOY_ROLE_ARN: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}

  rollback-frontend-netlify:
    name: Roll back Netlify storefront
    needs:
      - plan
      - production-approval
      - deploy-frontend-netlify
      - smoke-tests
    if: |
      always() &&
      needs.production-approval.result == 'success' &&
      (
        needs.plan.outputs.should_rollback_frontend_netlify == 'true' ||
        (
          github.event_name == 'workflow_dispatch' &&
          github.event.inputs.auto_rollback_on_smoke_failure == 'true' &&
          needs.deploy-frontend-netlify.result == 'success' &&
          needs.smoke-tests.result == 'failure'
        )
      )
    uses: ./.github/workflows/rollback-netlify.yml
    permissions:
      contents: read
    with:
      rollback_ref: ${{ needs.plan.outputs.rollback_netlify_ref != '' && needs.plan.outputs.rollback_netlify_ref || needs.deploy-frontend-netlify.outputs.netlify_rollback_ref }}
      parent_holds_production_lock: true
    secrets:
      NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

  rollback-frontend-aws:
    name: Roll back AWS storefront
    needs:
      - plan
      - production-approval
      - deploy-frontend-netlify
      - smoke-tests
    if: |
      always() &&
      needs.production-approval.result == 'success' &&
      (
        needs.plan.outputs.should_rollback_frontend_netlify == 'true' ||
        (
          github.event_name == 'workflow_dispatch' &&
          github.event.inputs.auto_rollback_on_smoke_failure == 'true' &&
          needs.deploy-frontend-netlify.result == 'success' &&
          needs.smoke-tests.result == 'failure'
        )
      )
    uses: ./.github/workflows/rollback-frontend-aws.yml
    permissions:
      contents: read
      id-token: write
    with:
      rollback_ref: ${{ needs.plan.outputs.rollback_aws_frontend_ref != '' && needs.plan.outputs.rollback_aws_frontend_ref || needs.deploy-frontend-netlify.outputs.aws_frontend_rollback_ref }}
      execute_rollback: true
      parent_holds_production_lock: true
    secrets:
      AWS_FRONTEND_DEPLOY_ROLE_ARN: ${{ secrets.AWS_FRONTEND_DEPLOY_ROLE_ARN }}

  rollback-frontend-vercel-storefront:
    name: Roll back Vercel storefront
    needs:
      - plan
      - production-approval
      - deploy-frontend-netlify
      - smoke-tests
    if: |
      always() &&
      needs.production-approval.result == 'success' &&
      (
        needs.plan.outputs.should_rollback_frontend_netlify == 'true' ||
        (
          github.event_name == 'workflow_dispatch' &&
          github.event.inputs.auto_rollback_on_smoke_failure == 'true' &&
          needs.deploy-frontend-netlify.result == 'success' &&
          needs.smoke-tests.result == 'failure'
        )
      )
    uses: ./.github/workflows/rollback-storefront-vercel.yml
    permissions:
      contents: read
    with:
      rollback_ref: ${{ needs.plan.outputs.rollback_vercel_storefront_ref != '' && needs.plan.outputs.rollback_vercel_storefront_ref || needs.deploy-frontend-netlify.outputs.vercel_storefront_rollback_ref }}
      parent_holds_production_lock: true
    secrets:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

  rollback-gateway:
    name: Roll back gateway
    needs:
      - plan
      - production-approval
      - deploy-gateway
      - smoke-tests
    if: |
      always() &&
      needs.production-approval.result == 'success' &&
      (
        needs.plan.outputs.should_rollback_gateway == 'true' ||
        (
          github.event_name == 'workflow_dispatch' &&
          github.event.inputs.auto_rollback_on_smoke_failure == 'true' &&
          needs.deploy-gateway.result == 'success' &&
          needs.smoke-tests.result == 'failure'
        )
      )
    uses: ./.github/workflows/rollback-gateway-vercel.yml
    permissions:
      contents: read
    with:
      rollback_ref: ${{ needs.plan.outputs.rollback_gateway_ref != '' && needs.plan.outputs.rollback_gateway_ref || needs.deploy-gateway.outputs.gateway_rollback_ref }}
      parent_holds_production_lock: true
    secrets:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

  rollback-decisions:
    name: Rollback decision summary
    runs-on: ubuntu-latest
    needs:
      - plan
      - smoke-tests
      - rollback-backend
      - rollback-frontend-netlify
      - rollback-frontend-vercel-storefront
      - rollback-frontend-aws
      - rollback-gateway
    if: always()

    steps:
      - name: Explain rollback decisions
        env:
          SHOULD_ROLLBACK_BACKEND: ${{ needs.plan.outputs.should_rollback_backend }}
          ROLLBACK_BACKEND_RESULT: ${{ needs.rollback-backend.result }}
          SHOULD_ROLLBACK_FRONTEND_NETLIFY: ${{ needs.plan.outputs.should_rollback_frontend_netlify }}
          ROLLBACK_FRONTEND_NETLIFY_RESULT: ${{ needs.rollback-frontend-netlify.result }}
          ROLLBACK_FRONTEND_VERCEL_RESULT: ${{ needs.rollback-frontend-vercel-storefront.result }}
          SHOULD_ROLLBACK_FRONTEND_AWS: ${{ needs.plan.outputs.should_rollback_frontend_netlify }}
          ROLLBACK_FRONTEND_AWS_RESULT: ${{ needs.rollback-frontend-aws.result }}
          SHOULD_ROLLBACK_GATEWAY: ${{ needs.plan.outputs.should_rollback_gateway }}
          ROLLBACK_GATEWAY_RESULT: ${{ needs.rollback-gateway.result }}
          IS_WORKFLOW_DISPATCH: ${{ github.event_name == 'workflow_dispatch' }}
          AUTO_ROLLBACK_INPUT: ${{ github.event.inputs.auto_rollback_on_smoke_failure == 'true' }}
          SMOKE_TESTS_RESULT: ${{ needs.smoke-tests.result }}
        run: |
          {
            echo "## Rollback Decisions"
            echo
            echo "| Rollback | Planned/manual | Result |"
            echo "|---|---:|---|"
            echo "| Backend | ${SHOULD_ROLLBACK_BACKEND} | ${ROLLBACK_BACKEND_RESULT} |"
            echo "| Netlify frontend | ${SHOULD_ROLLBACK_FRONTEND_NETLIFY} | ${ROLLBACK_FRONTEND_NETLIFY_RESULT} |"
            echo "| Vercel storefront | ${SHOULD_ROLLBACK_FRONTEND_NETLIFY} | ${ROLLBACK_FRONTEND_VERCEL_RESULT} |"
            echo "| AWS frontend | ${SHOULD_ROLLBACK_FRONTEND_AWS} | ${ROLLBACK_FRONTEND_AWS_RESULT} |"
            echo "| Gateway | ${SHOULD_ROLLBACK_GATEWAY} | ${ROLLBACK_GATEWAY_RESULT} |"
            echo
            echo "| Auto rollback condition | Value |"
            echo "|---|---|"
            echo "| Workflow dispatch | ${IS_WORKFLOW_DISPATCH} |"
            echo "| Auto rollback input | ${AUTO_ROLLBACK_INPUT} |"
            echo "| Smoke result | ${SMOKE_TESTS_RESULT} |"
            echo
            echo "Rollbacks only run for confirmed manual rollback requests or for workflow_dispatch smoke failures when auto rollback is explicitly enabled."
          } >> "$GITHUB_STEP_SUMMARY"

  release-desktop:
    name: Publish desktop apps
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - deploy-backend
      - deploy-frontend-netlify
      - deploy-frontend-aws
      - deploy-gateway
      - smoke-tests
    if: |
      always() &&
      needs.plan.outputs.should_release_desktop == 'true' &&
      needs.quality-gates.result == 'success' &&
      needs.security-gates.result == 'success' &&
      needs.production-approval.result == 'success' &&
      (needs.deploy-backend.result == 'success' || needs.deploy-backend.result == 'skipped') &&
      (needs.deploy-frontend-netlify.result == 'success' || needs.deploy-frontend-netlify.result == 'skipped') &&
      (needs.deploy-frontend-aws.result == 'success' || needs.deploy-frontend-aws.result == 'skipped') &&
      (needs.deploy-gateway.result == 'success' || needs.deploy-gateway.result == 'skipped') &&
      (needs.smoke-tests.result == 'success' || needs.smoke-tests.result == 'skipped')
    uses: ./.github/workflows/desktop-release.yml
    permissions:
      contents: write
    with:
      version: ${{ github.event.inputs.desktop_version || '' }}
      require_windows_signing: false
      require_macos_signing: false
      publish_store_release: false
    secrets:
      APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
      APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
      APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
      APPLE_ID: ${{ secrets.APPLE_ID }}
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_TRUSTED_SIGNING_ACCOUNT_NAME: ${{ secrets.AZURE_TRUSTED_SIGNING_ACCOUNT_NAME }}
      AZURE_TRUSTED_SIGNING_CERTIFICATE_PROFILE_NAME: ${{ secrets.AZURE_TRUSTED_SIGNING_CERTIFICATE_PROFILE_NAME }}
      AZURE_TRUSTED_SIGNING_ENDPOINT: ${{ secrets.AZURE_TRUSTED_SIGNING_ENDPOINT }}
      VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
      VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
      VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
      VITE_FIREBASE_CONFIG: ${{ secrets.VITE_FIREBASE_CONFIG }}
      VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}
      VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
      VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
      VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
      VITE_FIREBASE_WEB_CONFIG: ${{ secrets.VITE_FIREBASE_WEB_CONFIG }}
      WINDOWS_CERTIFICATE_BASE64: ${{ secrets.WINDOWS_CERTIFICATE_BASE64 }}
      WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
      WINDOWS_CODE_SIGNING_CERTIFICATE_BASE64: ${{ secrets.WINDOWS_CODE_SIGNING_CERTIFICATE_BASE64 }}
      WINDOWS_CODE_SIGNING_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CODE_SIGNING_CERTIFICATE_PASSWORD }}
      WINDOWS_CODE_SIGNING_PUBLISHER_NAME: ${{ secrets.WINDOWS_CODE_SIGNING_PUBLISHER_NAME }}

  release-desktop-noop:
    name: Desktop release no-op
    runs-on: ubuntu-latest
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - deploy-backend
      - deploy-frontend-netlify
      - deploy-frontend-aws
      - deploy-gateway
      - smoke-tests
      - release-desktop
    if: always() && needs.release-desktop.result == 'skipped'

    steps:
      - name: Explain desktop release decision
        env:
          EVENT_NAME: ${{ github.event_name }}
          CHANGED: ${{ needs.plan.outputs.desktop_changed }}
          SHOULD: ${{ needs.plan.outputs.should_release_desktop }}
          MANUAL_CONFIRMED: ${{ needs.plan.outputs.manual_confirmed }}
          MANUAL_REQUESTED: ${{ needs.plan.outputs.release_desktop_requested }}
          QUALITY_GATES_RESULT: ${{ needs.quality-gates.result }}
          SECURITY_GATES_RESULT: ${{ needs.security-gates.result }}
          APPROVAL_RESULT: ${{ needs.production-approval.result }}
          SMOKE_TESTS_RESULT: ${{ needs.smoke-tests.result }}
        run: |
          {
            echo "## Desktop Release No-Op"
            echo
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Event | ${EVENT_NAME} |"
            echo "| Changed | ${CHANGED} |"
            echo "| Manual requested | ${MANUAL_REQUESTED} |"
            echo "| Manual confirmed | ${MANUAL_CONFIRMED} |"
            echo "| Planned release | ${SHOULD} |"
            echo "| Quality gates | ${QUALITY_GATES_RESULT} |"
            echo "| Security gates | ${SECURITY_GATES_RESULT} |"
            echo "| Approval | ${APPROVAL_RESULT} |"
            echo "| Smoke tests | ${SMOKE_TESTS_RESULT} |"
            echo
            if [ "${EVENT_NAME}" = "workflow_dispatch" ] && [ "${MANUAL_REQUESTED}" = "true" ] && [ "${MANUAL_CONFIRMED}" != "true" ]; then
              echo "Desktop release was requested but no-opped because confirm_production was not PRODUCTION."
            elif [ "${SHOULD}" != "true" ]; then
              echo "Desktop release was not requested for this run."
            elif [ "${QUALITY_GATES_RESULT}" != "success" ] || [ "${SECURITY_GATES_RESULT}" != "success" ]; then
              echo "Desktop release was blocked because CI or security gates did not pass."
            elif [ "${SMOKE_TESTS_RESULT}" = "failure" ]; then
              echo "Desktop release was blocked because smoke tests failed."
            else
              echo "Desktop release did not run because approval or a dependent production job was not safe."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  release-mobile:
    name: Publish mobile apps
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - deploy-backend
      - deploy-frontend-netlify
      - deploy-frontend-aws
      - deploy-gateway
      - smoke-tests
    if: |
      always() &&
      needs.plan.outputs.should_release_mobile == 'true' &&
      needs.quality-gates.result == 'success' &&
      needs.security-gates.result == 'success' &&
      needs.production-approval.result == 'success' &&
      (needs.deploy-backend.result == 'success' || needs.deploy-backend.result == 'skipped') &&
      (needs.deploy-frontend-netlify.result == 'success' || needs.deploy-frontend-netlify.result == 'skipped') &&
      (needs.deploy-frontend-aws.result == 'success' || needs.deploy-frontend-aws.result == 'skipped') &&
      (needs.deploy-gateway.result == 'success' || needs.deploy-gateway.result == 'skipped') &&
      (needs.smoke-tests.result == 'success' || needs.smoke-tests.result == 'skipped')
    uses: ./.github/workflows/mobile-release.yml
    permissions:
      contents: write
    with:
      version: ${{ github.event.inputs.mobile_version || '' }}
      require_android_signing: false
      require_ios_signing: false
      publish_store_release: false
    secrets:
      ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
      ANDROID_RELEASE_KEYSTORE_BASE64: ${{ secrets.ANDROID_RELEASE_KEYSTORE_BASE64 }}
      ANDROID_RELEASE_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEYSTORE_PASSWORD }}
      ANDROID_RELEASE_KEY_ALIAS: ${{ secrets.ANDROID_RELEASE_KEY_ALIAS }}
      ANDROID_RELEASE_KEY_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEY_PASSWORD }}
      APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
      APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
      APPLE_PROVISIONING_PROFILE_BASE64: ${{ secrets.APPLE_PROVISIONING_PROFILE_BASE64 }}
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      APP_STORE_CONNECT_API_KEY_P8_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_P8_BASE64 }}
      APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
      APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
      AURA_ANDROID_DEFAULT_WEB_CLIENT_ID: ${{ secrets.AURA_ANDROID_DEFAULT_WEB_CLIENT_ID }}
      FIREBASE_ANDROID_GOOGLE_SERVICES_JSON_BASE64: ${{ secrets.FIREBASE_ANDROID_GOOGLE_SERVICES_JSON_BASE64 }}
      FIREBASE_IOS_GOOGLE_SERVICE_INFO_PLIST_BASE64: ${{ secrets.FIREBASE_IOS_GOOGLE_SERVICE_INFO_PLIST_BASE64 }}
      GOOGLE_PLAY_SERVICE_ACCOUNT_JSON_BASE64: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON_BASE64 }}
      IOS_PROVISIONING_PROFILE_BASE64: ${{ secrets.IOS_PROVISIONING_PROFILE_BASE64 }}
      IOS_PROVISIONING_PROFILE_SPECIFIER: ${{ secrets.IOS_PROVISIONING_PROFILE_SPECIFIER }}
      IOS_SIGNING_CERTIFICATE_P12_BASE64: ${{ secrets.IOS_SIGNING_CERTIFICATE_P12_BASE64 }}
      IOS_SIGNING_CERTIFICATE_PASSWORD: ${{ secrets.IOS_SIGNING_CERTIFICATE_PASSWORD }}
      IOS_TEAM_ID: ${{ secrets.IOS_TEAM_ID }}

  release-mobile-noop:
    name: Mobile release no-op
    runs-on: ubuntu-latest
    needs:
      - plan
      - quality-gates
      - security-gates
      - production-approval
      - deploy-backend
      - deploy-frontend-netlify
      - deploy-frontend-aws
      - deploy-gateway
      - smoke-tests
      - release-mobile
    if: always() && needs.release-mobile.result == 'skipped'

    steps:
      - name: Explain mobile release decision
        env:
          EVENT_NAME: ${{ github.event_name }}
          CHANGED: ${{ needs.plan.outputs.mobile_changed }}
          SHOULD: ${{ needs.plan.outputs.should_release_mobile }}
          MANUAL_CONFIRMED: ${{ needs.plan.outputs.manual_confirmed }}
          MANUAL_REQUESTED: ${{ needs.plan.outputs.release_mobile_requested }}
          QUALITY_GATES_RESULT: ${{ needs.quality-gates.result }}
          SECURITY_GATES_RESULT: ${{ needs.security-gates.result }}
          APPROVAL_RESULT: ${{ needs.production-approval.result }}
          SMOKE_TESTS_RESULT: ${{ needs.smoke-tests.result }}
        run: |
          {
            echo "## Mobile Release No-Op"
            echo
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Event | ${EVENT_NAME} |"
            echo "| Changed | ${CHANGED} |"
            echo "| Manual requested | ${MANUAL_REQUESTED} |"
            echo "| Manual confirmed | ${MANUAL_CONFIRMED} |"
            echo "| Planned release | ${SHOULD} |"
            echo "| Quality gates | ${QUALITY_GATES_RESULT} |"
            echo "| Security gates | ${SECURITY_GATES_RESULT} |"
            echo "| Approval | ${APPROVAL_RESULT} |"
            echo "| Smoke tests | ${SMOKE_TESTS_RESULT} |"
            echo
            if [ "${EVENT_NAME}" = "workflow_dispatch" ] && [ "${MANUAL_REQUESTED}" = "true" ] && [ "${MANUAL_CONFIRMED}" != "true" ]; then
              echo "Mobile release was requested but no-opped because confirm_production was not PRODUCTION."
            elif [ "${SHOULD}" != "true" ]; then
              echo "Mobile release was not requested for this run."
            elif [ "${QUALITY_GATES_RESULT}" != "success" ] || [ "${SECURITY_GATES_RESULT}" != "success" ]; then
              echo "Mobile release was blocked because CI or security gates did not pass."
            elif [ "${SMOKE_TESTS_RESULT}" = "failure" ]; then
              echo "Mobile release was blocked because smoke tests failed."
            else
              echo "Mobile release did not run because approval or a dependent production job was not safe."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  production-tag:
    name: Create production traceability tag
    runs-on: ubuntu-latest
    needs:
      - deploy-backend
      - deploy-frontend-netlify
      - deploy-frontend-aws
      - deploy-gateway
      - smoke-tests
      - release-desktop
      - release-mobile
    if: |
      always() &&
      (
        needs.deploy-backend.result == 'success' ||
        needs.deploy-frontend-netlify.result == 'success' ||
        needs.deploy-frontend-aws.result == 'success' ||
        needs.deploy-gateway.result == 'success' ||
        needs.release-desktop.result == 'success' ||
        needs.release-mobile.result == 'success'
      ) &&
      (needs.smoke-tests.result == 'success' || needs.smoke-tests.result == 'skipped')
    permissions:
      contents: write
    outputs:
      tag: ${{ steps.tag.outputs.tag }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Create and push production tag
        id: tag
        run: |
          set -euo pipefail

          short_sha="${GITHUB_SHA::7}"
          timestamp="$(date -u +'%Y%m%d%H%M%S')"
          tag="production-${timestamp}-${short_sha}"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git tag -a "${tag}" -m "Production release ${tag} from ${GITHUB_SHA}"
          git push origin "${tag}"

          echo "tag=${tag}" >> "$GITHUB_OUTPUT"
          echo "Created production tag: ${tag}"

  production-summary:
    name: Production release summary
    runs-on: ubuntu-latest
    if: always()
    needs:
      - plan
      - quality-gates
      - security-gates
      - release-safety-gates
      - production-approval
      - production-approval-noop
      - release-manifest
      - deploy-backend
      - deploy-backend-noop
      - deploy-frontend-netlify
      - deploy-frontend-netlify-noop
      - deploy-frontend-aws
      - deploy-frontend-aws-noop
      - deploy-gateway
      - deploy-gateway-noop
      - smoke-tests
      - smoke-tests-noop
      - rollback-backend
      - rollback-frontend-netlify
      - rollback-frontend-vercel-storefront
      - rollback-frontend-aws
      - rollback-gateway
      - rollback-decisions
      - release-desktop
      - release-desktop-noop
      - release-mobile
      - release-mobile-noop
      - production-tag

    steps:
      - name: Write production summary
        env:
          EVENT_NAME: ${{ github.event_name }}
          ACTOR_NAME: ${{ github.actor }}
          REF_NAME: ${{ github.ref }}
          COMMIT_SHA: ${{ github.sha }}
          RUN_ID: ${{ github.run_id }}
          REQUESTED_PRODUCTION_ACTION: ${{ needs.plan.outputs.requested_production_action }}
          PLANNED_PRODUCTION_ACTION: ${{ needs.plan.outputs.planned_production_action }}
          MANUAL_CONFIRMED: ${{ needs.plan.outputs.manual_confirmed }}
          SHOULD_RUN_SMOKE_TESTS: ${{ needs.plan.outputs.should_run_smoke_tests }}
          BACKEND_READY: ${{ needs.plan.outputs.backend_ready }}
          BACKEND_MISSING: ${{ needs.plan.outputs.backend_missing }}
          FRONTEND_NETLIFY_READY: ${{ needs.plan.outputs.frontend_netlify_ready }}
          FRONTEND_NETLIFY_MISSING: ${{ needs.plan.outputs.frontend_netlify_missing }}
          FRONTEND_AWS_READY: ${{ needs.plan.outputs.frontend_aws_ready }}
          FRONTEND_AWS_MISSING: ${{ needs.plan.outputs.frontend_aws_missing }}
          GATEWAY_READY: ${{ needs.plan.outputs.gateway_ready }}
          GATEWAY_MISSING: ${{ needs.plan.outputs.gateway_missing }}
          QUALITY_GATES_RESULT: ${{ needs.quality-gates.result }}
          SECURITY_GATES_RESULT: ${{ needs.security-gates.result }}
          RELEASE_SAFETY_GATES_RESULT: ${{ needs.release-safety-gates.result }}
          PRODUCTION_APPROVAL_RESULT: ${{ needs.production-approval.result }}
          RELEASE_MANIFEST_RESULT: ${{ needs.release-manifest.result }}
          SHOULD_DEPLOY_BACKEND: ${{ needs.plan.outputs.should_deploy_backend }}
          DEPLOY_BACKEND_RESULT: ${{ needs.deploy-backend.result }}
          DEPLOY_BACKEND_NOOP_RESULT: ${{ needs.deploy-backend-noop.result }}
          SHOULD_DEPLOY_FRONTEND_NETLIFY: ${{ needs.plan.outputs.should_deploy_frontend_netlify }}
          DEPLOY_FRONTEND_NETLIFY_RESULT: ${{ needs.deploy-frontend-netlify.result }}
          DEPLOY_FRONTEND_NETLIFY_NOOP_RESULT: ${{ needs.deploy-frontend-netlify-noop.result }}
          SHOULD_DEPLOY_FRONTEND_AWS: ${{ needs.plan.outputs.should_deploy_frontend_aws }}
          DEPLOY_FRONTEND_AWS_RESULT: ${{ needs.deploy-frontend-aws.result }}
          DEPLOY_FRONTEND_AWS_NOOP_RESULT: ${{ needs.deploy-frontend-aws-noop.result }}
          SHOULD_DEPLOY_GATEWAY: ${{ needs.plan.outputs.should_deploy_gateway }}
          DEPLOY_GATEWAY_RESULT: ${{ needs.deploy-gateway.result }}
          DEPLOY_GATEWAY_NOOP_RESULT: ${{ needs.deploy-gateway-noop.result }}
          SMOKE_TESTS_RESULT: ${{ needs.smoke-tests.result }}
          SMOKE_TESTS_NOOP_RESULT: ${{ needs.smoke-tests-noop.result }}
          SHOULD_ROLLBACK_BACKEND: ${{ needs.plan.outputs.should_rollback_backend }}
          ROLLBACK_BACKEND_RESULT: ${{ needs.rollback-backend.result }}
          SHOULD_ROLLBACK_FRONTEND_NETLIFY: ${{ needs.plan.outputs.should_rollback_frontend_netlify }}
          ROLLBACK_FRONTEND_NETLIFY_RESULT: ${{ needs.rollback-frontend-netlify.result }}
          ROLLBACK_FRONTEND_VERCEL_RESULT: ${{ needs.rollback-frontend-vercel-storefront.result }}
          SHOULD_ROLLBACK_FRONTEND_AWS: ${{ needs.plan.outputs.should_rollback_frontend_netlify }}
          ROLLBACK_FRONTEND_AWS_RESULT: ${{ needs.rollback-frontend-aws.result }}
          SHOULD_ROLLBACK_GATEWAY: ${{ needs.plan.outputs.should_rollback_gateway }}
          ROLLBACK_GATEWAY_RESULT: ${{ needs.rollback-gateway.result }}
          SHOULD_RELEASE_DESKTOP: ${{ needs.plan.outputs.should_release_desktop }}
          RELEASE_DESKTOP_RESULT: ${{ needs.release-desktop.result }}
          RELEASE_DESKTOP_NOOP_RESULT: ${{ needs.release-desktop-noop.result }}
          SHOULD_RELEASE_MOBILE: ${{ needs.plan.outputs.should_release_mobile }}
          RELEASE_MOBILE_RESULT: ${{ needs.release-mobile.result }}
          RELEASE_MOBILE_NOOP_RESULT: ${{ needs.release-mobile-noop.result }}
          SIGNING_ENABLED: ${{ needs.plan.outputs.signing_enabled }}
          STORE_PUBLISH_ENABLED: ${{ needs.plan.outputs.store_publish_enabled }}
          PRODUCTION_TAG_RESULT: ${{ needs.production-tag.result }}
          PRODUCTION_TAG: ${{ needs.production-tag.outputs.tag }}
        run: |
          {
            echo "## Aura Production Release Summary"
            echo
            echo "### A. Run metadata"
            echo
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Event | ${EVENT_NAME} |"
            echo "| Actor | ${ACTOR_NAME} |"
            echo "| Ref | ${REF_NAME} |"
            echo "| SHA | ${COMMIT_SHA} |"
            echo "| Run | ${RUN_ID} |"
            echo
            echo "### B. Plan decisions"
            echo
            echo "| Decision | Value |"
            echo "|---|---|"
            echo "| Requested production action | ${REQUESTED_PRODUCTION_ACTION} |"
            echo "| Planned production action | ${PLANNED_PRODUCTION_ACTION} |"
            echo "| Manual confirmed | ${MANUAL_CONFIRMED} |"
            echo "| Should run smoke tests | ${SHOULD_RUN_SMOKE_TESTS} |"
            echo
            echo "### C. Provider readiness"
            echo
            echo "| Provider | Ready | Missing config names |"
            echo "|---|---:|---|"
            echo "| Backend AWS | ${BACKEND_READY} | ${BACKEND_MISSING} |"
            echo "| Frontend multihost | ${FRONTEND_NETLIFY_READY} | ${FRONTEND_NETLIFY_MISSING} |"
            echo "| Gateway Vercel | ${GATEWAY_READY} | ${GATEWAY_MISSING} |"
            echo
            echo "### D. CI/security/approval results"
            echo
            echo "| Gate | Result |"
            echo "|---|---|"
            echo "| Quality gates | ${QUALITY_GATES_RESULT} |"
            echo "| Security gates | ${SECURITY_GATES_RESULT} |"
            echo "| Same-SHA release safety gates | ${RELEASE_SAFETY_GATES_RESULT} |"
            echo "| Production approval | ${PRODUCTION_APPROVAL_RESULT} |"
            echo "| Release manifest | ${RELEASE_MANIFEST_RESULT} |"
            echo
            echo "### E. Deploy decisions/results"
            echo
            echo "| Deploy | Planned | Result | No-op job |"
            echo "|---|---:|---|---|"
            echo "| Backend AWS | ${SHOULD_DEPLOY_BACKEND} | ${DEPLOY_BACKEND_RESULT} | ${DEPLOY_BACKEND_NOOP_RESULT} |"
            echo "| Frontend multihost | ${SHOULD_DEPLOY_FRONTEND_NETLIFY} | ${DEPLOY_FRONTEND_NETLIFY_RESULT} | ${DEPLOY_FRONTEND_NETLIFY_NOOP_RESULT} |"
            echo "| Gateway Vercel | ${SHOULD_DEPLOY_GATEWAY} | ${DEPLOY_GATEWAY_RESULT} | ${DEPLOY_GATEWAY_NOOP_RESULT} |"
            echo
            echo "### F. Smoke test result"
            echo
            echo "| Job | Result |"
            echo "|---|---|"
            echo "| Smoke tests | ${SMOKE_TESTS_RESULT} |"
            echo "| Smoke tests no-op | ${SMOKE_TESTS_NOOP_RESULT} |"
            echo
            echo "### G. Rollback decisions/results"
            echo
            echo "| Rollback | Planned | Result |"
            echo "|---|---:|---|"
            echo "| Backend | ${SHOULD_ROLLBACK_BACKEND} | ${ROLLBACK_BACKEND_RESULT} |"
            echo "| Frontend Netlify | ${SHOULD_ROLLBACK_FRONTEND_NETLIFY} | ${ROLLBACK_FRONTEND_NETLIFY_RESULT} |"
            echo "| Frontend Vercel | ${SHOULD_ROLLBACK_FRONTEND_NETLIFY} | ${ROLLBACK_FRONTEND_VERCEL_RESULT} |"
            echo "| Frontend AWS | ${SHOULD_ROLLBACK_FRONTEND_AWS} | ${ROLLBACK_FRONTEND_AWS_RESULT} |"
            echo "| Gateway | ${SHOULD_ROLLBACK_GATEWAY} | ${ROLLBACK_GATEWAY_RESULT} |"
            echo
            echo "### H. Desktop/mobile release result"
            echo
            echo "| Release | Planned | Result | No-op job |"
            echo "|---|---:|---|---|"
            echo "| Desktop | ${SHOULD_RELEASE_DESKTOP} | ${RELEASE_DESKTOP_RESULT} | ${RELEASE_DESKTOP_NOOP_RESULT} |"
            echo "| Mobile | ${SHOULD_RELEASE_MOBILE} | ${RELEASE_MOBILE_RESULT} | ${RELEASE_MOBILE_NOOP_RESULT} |"
            echo
            echo "### I. Signing status"
            echo
            echo "| Platform | Required | Enabled |"
            echo "|---|---:|---:|"
            echo "| Windows | false | ${SIGNING_ENABLED} |"
            echo "| macOS | false | ${SIGNING_ENABLED} |"
            echo "| Android | false | ${SIGNING_ENABLED} |"
            echo "| iOS | false | ${SIGNING_ENABLED} |"
            echo
            echo "### J. Store publishing status"
            echo
            echo "| Store publishing | Enabled |"
            echo "|---|---:|"
            echo "| Microsoft Store / Mac App Store / Google Play / App Store | ${STORE_PUBLISH_ENABLED} |"
            echo
            echo "### K. Production tag"
            echo
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Tag job result | ${PRODUCTION_TAG_RESULT} |"
            echo "| Tag | ${PRODUCTION_TAG} |"
          } >> "$GITHUB_STEP_SUMMARY"
