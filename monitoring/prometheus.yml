global:
  scrape_interval: 30s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: aura-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - aura-api:5000
