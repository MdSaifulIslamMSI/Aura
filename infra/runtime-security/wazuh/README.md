# Wazuh Runtime Notes

Use Wazuh for host integrity, package drift, and authentication event collection around Aura production nodes.

Do not store credentials in agent config. Send only telemetry needed for security operations.
