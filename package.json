{
  "private": true,
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "azure:runtime:publish": "powershell -ExecutionPolicy Bypass -File infra/github/publish-azure-runtime-env.ps1 -TriggerWorkflow"
  }
}
